# -------------------------------------------------------
# EduTrack — Local Development Environment
# -------------------------------------------------------

# PRISMA ORM — Database URLs
# Replace [YOUR-PASSWORD] with your actual Supabase database password.

# Connect to Postgres via the shared transaction-mode pooler (IPv4-only)
# Used by Prisma Client at runtime
DATABASE_URL="postgresql://postgres.umvbyywkojuxnxgkuwbt:Sathish%400086@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Connect to Postgres via the shared session-mode pooler
# Used for Prisma migrations (db push / db pull / migrate deploy)
DIRECT_URL="postgresql://postgres.umvbyywkojuxnxgkuwbt:Sathish%400086@aws-1-ap-south-1.pooler.supabase.com:5432/postgres"
