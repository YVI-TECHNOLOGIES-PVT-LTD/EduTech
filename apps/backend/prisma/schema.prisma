generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
  schemas   = ["auth", "public"]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model custom_oauth_providers {
  id                      String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider_type           String
  identifier              String    @unique
  name                    String
  client_id               String
  client_secret           String
  acceptable_client_ids   String[]  @default([])
  scopes                  String[]  @default([])
  pkce_enabled            Boolean   @default(true)
  attribute_mapping       Json      @default("{}")
  authorization_params    Json      @default("{}")
  enabled                 Boolean   @default(true)
  email_optional          Boolean   @default(false)
  issuer                  String?
  discovery_url           String?
  skip_nonce_check        Boolean   @default(false)
  cached_discovery        Json?
  discovery_cached_at     DateTime? @db.Timestamptz(6)
  authorization_url       String?
  token_url               String?
  userinfo_url            String?
  jwks_uri                String?
  created_at              DateTime  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime  @default(now()) @db.Timestamptz(6)
  custom_claims_allowlist String[]  @default([])

  @@index([created_at])
  @@index([enabled])
  @@index([identifier])
  @@index([provider_type])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                 @id @db.Uuid
  user_id                String?                @db.Uuid
  auth_code              String?
  code_challenge_method  code_challenge_method?
  code_challenge         String?
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?              @db.Timestamptz(6)
  updated_at             DateTime?              @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?              @db.Timestamptz(6)
  invite_token           String?
  referrer               String?
  oauth_client_state_id  String?                @db.Uuid
  linking_target_id      String?                @db.Uuid
  email_optional         Boolean                @default(false)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String     @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime?  @db.Timestamptz(6)
  created_at      DateTime?  @db.Timestamptz(6)
  updated_at      DateTime?  @db.Timestamptz(6)
  email           String?    @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String           @id @db.Uuid
  user_id                      String           @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime         @db.Timestamptz(6)
  updated_at                   DateTime         @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?        @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?          @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        auth_users       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 auth_users?                @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                         String                  @id @db.Uuid
  client_secret_hash         String?
  registration_type          oauth_registration_type
  redirect_uris              String
  grant_types                String
  client_name                String?
  client_uri                 String?
  logo_uri                   String?
  created_at                 DateTime                @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at                 DateTime?               @db.Timestamptz(6)
  client_type                oauth_client_type       @default(confidential)
  token_endpoint_auth_method String
  oauth_authorizations       oauth_authorizations[]
  oauth_consents             oauth_consents[]
  sessions                   sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         auth_users    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  users      auth_users          @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  auth_users       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([oauth_client_id])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model auth_users {
  instance_id                                                  String?                       @db.Uuid
  id                                                           String                        @id @db.Uuid
  aud                                                          String?                       @db.VarChar(255)
  role                                                         String?                       @db.VarChar(255)
  email                                                        String?                       @db.VarChar(255)
  encrypted_password                                           String?                       @db.VarChar(255)
  email_confirmed_at                                           DateTime?                     @db.Timestamptz(6)
  invited_at                                                   DateTime?                     @db.Timestamptz(6)
  confirmation_token                                           String?                       @db.VarChar(255)
  confirmation_sent_at                                         DateTime?                     @db.Timestamptz(6)
  recovery_token                                               String?                       @db.VarChar(255)
  recovery_sent_at                                             DateTime?                     @db.Timestamptz(6)
  email_change_token_new                                       String?                       @db.VarChar(255)
  email_change                                                 String?                       @db.VarChar(255)
  email_change_sent_at                                         DateTime?                     @db.Timestamptz(6)
  last_sign_in_at                                              DateTime?                     @db.Timestamptz(6)
  raw_app_meta_data                                            Json?
  raw_user_meta_data                                           Json?
  is_super_admin                                               Boolean?
  created_at                                                   DateTime?                     @db.Timestamptz(6)
  updated_at                                                   DateTime?                     @db.Timestamptz(6)
  phone                                                        String?                       @unique
  phone_confirmed_at                                           DateTime?                     @db.Timestamptz(6)
  phone_change                                                 String?                       @default("")
  phone_change_token                                           String?                       @default("") @db.VarChar(255)
  phone_change_sent_at                                         DateTime?                     @db.Timestamptz(6)
  confirmed_at                                                 DateTime?                     @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current                                   String?                       @default("") @db.VarChar(255)
  email_change_confirm_status                                  Int?                          @default(0) @db.SmallInt
  banned_until                                                 DateTime?                     @db.Timestamptz(6)
  reauthentication_token                                       String?                       @default("") @db.VarChar(255)
  reauthentication_sent_at                                     DateTime?                     @db.Timestamptz(6)
  is_sso_user                                                  Boolean                       @default(false)
  deleted_at                                                   DateTime?                     @db.Timestamptz(6)
  is_anonymous                                                 Boolean                       @default(false)
  identities                                                   identities[]
  mfa_factors                                                  mfa_factors[]
  oauth_authorizations                                         oauth_authorizations[]
  oauth_consents                                               oauth_consents[]
  one_time_tokens                                              one_time_tokens[]
  sessions                                                     sessions[]
  webauthn_challenges                                          webauthn_challenges[]
  webauthn_credentials                                         webauthn_credentials[]
  admission_fees                                               admission_fees[]
  fee_receipts_fee_receipts_collected_byTousers                fee_receipts[]                @relation("fee_receipts_collected_byTousers")
  fee_receipts_fee_receipts_generated_byTousers                fee_receipts[]                @relation("fee_receipts_generated_byTousers")
  finance_audit_logs                                           finance_audit_logs[]
  finance_fee_structures                                       finance_fee_structures[]
  finance_settings                                             finance_settings[]
  notifications                                                notifications[]
  payment_transactions_payment_transactions_cashier_idTousers  payment_transactions[]        @relation("payment_transactions_cashier_idTousers")
  payment_transactions_payment_transactions_verified_byTousers payment_transactions[]        @relation("payment_transactions_verified_byTousers")
  student_attendance_cache                                     student_attendance_cache[]
  student_fee_clearance_cache                                  student_fee_clearance_cache[]
  student_fee_ledger                                           student_fee_ledger[]
  user_roles                                                   user_roles[]
  profile                                                      public_users?

  @@index([created_at(sort: Desc)], map: "idx_users_created_at_desc")
  @@index([email], map: "idx_users_email")
  @@index([last_sign_in_at(sort: Desc)], map: "idx_users_last_sign_in_at_desc")
  @@index([instance_id])
  @@index([is_anonymous])
  @@map("users")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model webauthn_challenges {
  id             String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String?     @db.Uuid
  challenge_type String
  session_data   Json
  created_at     DateTime    @default(now()) @db.Timestamptz(6)
  expires_at     DateTime    @db.Timestamptz(6)
  users          auth_users? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([expires_at])
  @@index([user_id])
  @@schema("auth")
}

model webauthn_credentials {
  id               String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id          String     @db.Uuid
  credential_id    Bytes      @unique
  public_key       Bytes
  attestation_type String     @default("")
  aaguid           String?    @db.Uuid
  sign_count       BigInt     @default(0)
  transports       Json       @default("[]")
  backup_eligible  Boolean    @default(false)
  backed_up        Boolean    @default(false)
  friendly_name    String     @default("")
  created_at       DateTime   @default(now()) @db.Timestamptz(6)
  updated_at       DateTime   @default(now()) @db.Timestamptz(6)
  last_used_at     DateTime?  @db.Timestamptz(6)
  users            auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id])
  @@schema("auth")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model academic_automation_logs {
  id           String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id    String?       @db.Uuid
  action       String
  details      Json?
  performed_by String?       @db.Uuid
  created_at   DateTime?     @default(now()) @db.Timestamptz(6)
  users        public_users? @relation(fields: [performed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  schools      schools?      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model academic_standing_rules {
  id               String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String  @db.Uuid
  min_gpa          Decimal @default(5.00) @db.Decimal(4, 2)
  max_backlogs     Int     @default(0)
  resulting_status String
  schools          schools @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model academic_years {
  id                                                                          String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                                                                   String?                         @db.Uuid
  year_label                                                                  String
  is_active                                                                   Boolean?                        @default(false)
  created_at                                                                  DateTime?                       @default(now()) @db.Timestamptz(6)
  status                                                                      String?                         @default("DRAFT")
  start_date                                                                  DateTime?                       @db.Date
  end_date                                                                    DateTime?                       @db.Date
  schools                                                                     schools?                        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_applications                                                      admission_applications[]
  admission_conversion_metrics                                                admission_conversion_metrics[]
  admission_enquiries                                                         admission_enquiries[]
  admission_exam_schedule                                                     admission_exam_schedule[]
  admission_fee_structures                                                    admission_fee_structures[]
  admission_merit_rules                                                       admission_merit_rules[]
  admissions                                                                  admissions[]
  assessment_sessions                                                         assessment_sessions[]
  classes                                                                     classes[]
  document_checklists                                                         document_checklists[]
  exams                                                                       exams[]
  finance_fee_structures                                                      finance_fee_structures[]
  result_publications                                                         result_publications[]
  result_rules                                                                result_rules[]
  section_faculty_assignments                                                 section_faculty_assignments[]
  student_academic_records                                                    student_academic_records[]
  student_attendance_cache                                                    student_attendance_cache[]
  student_attendance_summary                                                  student_attendance_summary[]
  student_class_allocations                                                   student_class_allocations[]
  student_faculty_assignments                                                 student_faculty_assignments[]
  student_fee_clearance_cache                                                 student_fee_clearance_cache[]
  student_promotions_student_promotions_from_academic_year_idToacademic_years student_promotions[]            @relation("student_promotions_from_academic_year_idToacademic_years")
  student_promotions_student_promotions_to_academic_year_idToacademic_years   student_promotions[]            @relation("student_promotions_to_academic_year_idToacademic_years")
  student_roll_number_sequences                                               student_roll_number_sequences[]
  student_section_history                                                     student_section_history[]
  student_sections                                                            student_sections[]
  student_subject_results                                                     student_subject_results[]
  student_year_summaries                                                      student_year_summaries[]
  students                                                                    students[]
  timetable_slots                                                             timetable_slots[]
  transport_fee_slabs                                                         transport_fee_slabs[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_age_rules {
  id         String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  grade      String    @unique
  min_age    Int
  max_age    Int
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_applications {
  id                                                                                                      String                              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                                                                                               String                              @db.Uuid
  academic_year_id                                                                                        String                              @db.Uuid
  lead_id                                                                                                 String?                             @db.Uuid
  status                                                                                                  String                              @default("DRAFT")
  version                                                                                                 Int                                 @default(1)
  is_current                                                                                              Boolean                             @default(true)
  created_by                                                                                              String?                             @db.Uuid
  change_reason                                                                                           String?
  submitted_at                                                                                            DateTime?                           @db.Timestamptz(6)
  created_at                                                                                              DateTime?                           @default(now()) @db.Timestamptz(6)
  updated_at                                                                                              DateTime?                           @default(now()) @db.Timestamptz(6)
  deleted_at                                                                                              DateTime?                           @db.Timestamptz(6)
  academic_years                                                                                          academic_years                      @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                                                                                                   public_users?                       @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  admission_leads                                                                                         admission_leads?                    @relation(fields: [lead_id], references: [id], onUpdate: NoAction)
  schools                                                                                                 schools                             @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_confirmation                                                                                  admission_confirmation?
  admission_enrollment_logs                                                                               admission_enrollment_logs[]
  admission_exam_session_candidates                                                                       admission_exam_session_candidates[]
  admission_fee_assignments                                                                               admission_fee_assignments[]
  admission_fee_waivers                                                                                   admission_fee_waivers[]
  admission_hall_tickets                                                                                  admission_hall_tickets[]
  admission_interviews                                                                                    admission_interviews?
  admission_merit_results                                                                                 admission_merit_results?
  admission_offer_letters                                                                                 admission_offer_letters?
  admission_payments                                                                                      admission_payments[]
  admission_waitlist_promotions_admission_waitlist_promotions_from_application_idToadmission_applications admission_waitlist_promotions[]     @relation("admission_waitlist_promotions_from_application_idToadmission_applications")
  admission_waitlist_promotions_admission_waitlist_promotions_to_application_idToadmission_applications   admission_waitlist_promotions[]     @relation("admission_waitlist_promotions_to_application_idToadmission_applications")
  application_declarations                                                                                application_declarations[]
  application_documents                                                                                   application_documents[]
  application_parents                                                                                     application_parents[]
  application_preferences                                                                                 application_preferences[]
  application_previous_education                                                                          application_previous_education[]
  application_profiles                                                                                    application_profiles[]
  application_workflow                                                                                    application_workflow[]
  fee_reminders                                                                                           fee_reminders[]
  payment_transactions                                                                                    payment_transactions[]
  student_fee_ledger                                                                                      student_fee_ledger[]
  student_provisioning_jobs                                                                               student_provisioning_jobs[]
  students                                                                                                students[]

  @@index([lead_id], map: "idx_applications_lead")
  @@index([status], map: "idx_applications_status")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_attempts {
  id                             String                           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                      String                           @db.Uuid
  session_id                     String                           @db.Uuid
  snapshot_id                    String                           @db.Uuid
  start_time                     DateTime?                        @default(now()) @db.Timestamptz(6)
  submit_time                    DateTime?                        @db.Timestamptz(6)
  status                         String?                          @default("STARTED")
  evaluation_status              String?                          @default("DRAFT")
  score_obtained                 Decimal?                         @db.Decimal(5, 2)
  created_at                     DateTime?                        @default(now()) @db.Timestamptz(6)
  schools                        schools                          @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_assessment_sessions  admission_assessment_sessions    @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_assessment_snapshots admission_assessment_snapshots   @relation(fields: [snapshot_id], references: [id], onUpdate: NoAction)
  admission_assessment_responses admission_assessment_responses[]

  @@index([session_id], map: "idx_assessment_attempts_session")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_events {
  id                            String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                    String                        @db.Uuid
  event_type                    String
  details                       Json?
  logged_at                     DateTime?                     @default(now()) @db.Timestamptz(6)
  admission_assessment_sessions admission_assessment_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([session_id], map: "idx_assessment_events_session")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_outbox {
  id           String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id    String    @db.Uuid
  event_type   String
  payload      Json
  status       String?   @default("PENDING")
  error_log    String?
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  processed_at DateTime? @db.Timestamptz(6)
  schools      schools   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([status], map: "idx_assessment_outbox_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_policies {
  id                      String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  schedule_id             String                  @unique @db.Uuid
  school_id               String                  @db.Uuid
  allow_early_login_mins  Int?                    @default(15)
  grace_period_mins       Int?                    @default(10)
  late_entry_mins         Int?                    @default(15)
  allow_resume            Boolean?                @default(true)
  auto_submit_on_expiry   Boolean?                @default(true)
  max_attempts            Int?                    @default(1)
  created_at              DateTime?               @default(now()) @db.Timestamptz(6)
  admission_exam_schedule admission_exam_schedule @relation(fields: [schedule_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                 schools                 @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_question_mapping {
  section_id                    String                        @db.Uuid
  question_id                   String                        @db.Uuid
  sort_order                    Int                           @default(1)
  admission_question_bank       admission_question_bank       @relation(fields: [question_id], references: [id], onUpdate: NoAction)
  admission_assessment_sections admission_assessment_sections @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([section_id, question_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_responses {
  id                                             String                                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  attempt_id                                     String                                          @db.Uuid
  snapshot_question_id                           String                                          @db.Uuid
  selected_option_id                             String?                                         @db.Uuid
  text_answer                                    String?
  time_spent_seconds                             Int?                                            @default(0)
  marks_awarded                                  Decimal?                                        @db.Decimal(5, 2)
  is_correct                                     Boolean?
  graded_by                                      String?                                         @db.Uuid
  graded_at                                      DateTime?                                       @db.Timestamptz(6)
  admission_assessment_attempts                  admission_assessment_attempts                   @relation(fields: [attempt_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                                          public_users?                                   @relation(fields: [graded_by], references: [id], onUpdate: NoAction)
  admission_assessment_snapshot_question_options admission_assessment_snapshot_question_options? @relation(fields: [selected_option_id], references: [id], onUpdate: NoAction)
  admission_assessment_snapshot_questions        admission_assessment_snapshot_questions         @relation(fields: [snapshot_question_id], references: [id], onUpdate: NoAction)

  @@unique([attempt_id, snapshot_question_id], map: "unique_attempt_snap_question")
  @@index([attempt_id], map: "idx_assessment_responses_attempt")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_sections {
  id                                    String                                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id                           String                                  @db.Uuid
  section_name                          String
  max_marks                             Decimal                                 @db.Decimal(5, 2)
  sort_order                            Int                                     @default(1)
  created_at                            DateTime?                               @default(now()) @db.Timestamptz(6)
  admission_assessment_question_mapping admission_assessment_question_mapping[]
  admission_exam_templates              admission_exam_templates                @relation(fields: [template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_sessions {
  id                                String                            @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                         String                            @db.Uuid
  candidate_allocation_id           String                            @db.Uuid
  otp_hash                          String
  otp_expires_at                    DateTime                          @db.Timestamptz(6)
  exam_token_hash                   String?
  ip_address                        String?
  browser_agent                     String?
  last_heartbeat_at                 DateTime?                         @db.Timestamptz(6)
  status                            String?                           @default("CREATED")
  created_at                        DateTime?                         @default(now()) @db.Timestamptz(6)
  admission_assessment_attempts     admission_assessment_attempts[]
  admission_assessment_events       admission_assessment_events[]
  admission_exam_session_candidates admission_exam_session_candidates @relation(fields: [candidate_allocation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                           schools                           @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([candidate_allocation_id], map: "idx_assessment_sessions_candidate")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_snapshot_question_options {
  id                                      String                                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  snapshot_question_id                    String                                  @db.Uuid
  option_text                             String
  is_correct                              Boolean                                 @default(false)
  admission_assessment_responses          admission_assessment_responses[]
  admission_assessment_snapshot_questions admission_assessment_snapshot_questions @relation(fields: [snapshot_question_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "admission_assessment_snapshot_questio_snapshot_question_id_fkey")

  @@index([snapshot_question_id], map: "idx_assessment_snapshot_options_question")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_snapshot_questions {
  id                                             String                                           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  snapshot_id                                    String                                           @db.Uuid
  question_id                                    String                                           @db.Uuid
  section_name                                   String
  question_text                                  String
  question_type                                  String
  points                                         Decimal                                          @db.Decimal(5, 2)
  negative_marks                                 Decimal                                          @db.Decimal(5, 2)
  sort_order                                     Int
  admission_assessment_responses                 admission_assessment_responses[]
  admission_assessment_snapshot_question_options admission_assessment_snapshot_question_options[]
  admission_assessment_snapshots                 admission_assessment_snapshots                   @relation(fields: [snapshot_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([snapshot_id], map: "idx_assessment_snapshot_questions_snap")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_assessment_snapshots {
  id                                      String                                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                               String                                    @db.Uuid
  template_id                             String?                                   @db.Uuid
  name                                    String
  grade                                   String
  version                                 Int
  duration                                Int
  total_marks                             Decimal                                   @db.Decimal(5, 2)
  passing_marks                           Decimal                                   @db.Decimal(5, 2)
  evaluation_type                         String
  created_at                              DateTime?                                 @default(now()) @db.Timestamptz(6)
  admission_assessment_attempts           admission_assessment_attempts[]
  admission_assessment_snapshot_questions admission_assessment_snapshot_questions[]
  schools                                 schools                                   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_exam_templates                admission_exam_templates?                 @relation(fields: [template_id], references: [id], onUpdate: NoAction)

  @@index([template_id], map: "idx_assessment_snapshots_template")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_audit_logs {
  id           String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  admission_id String        @db.Uuid
  action       String
  performed_by String?       @db.Uuid
  remarks      String?
  created_at   DateTime?     @default(now()) @db.Timestamptz(6)
  admissions   admissions    @relation(fields: [admission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users        public_users? @relation(fields: [performed_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_confirmation {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @unique @db.Uuid
  student_id             String?                @db.Uuid
  admission_number       String                 @unique
  confirmed_at           DateTime?              @default(now()) @db.Timestamptz(6)
  confirmed_by           String?                @db.Uuid
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  public_users?          @relation(fields: [confirmed_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_conversion_metrics {
  id                 String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String         @db.Uuid
  academic_year_id   String         @db.Uuid
  total_leads        Int?           @default(0)
  total_applications Int?           @default(0)
  total_admissions   Int?           @default(0)
  created_at         DateTime?      @default(now()) @db.Timestamptz(6)
  academic_years     academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_documents {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  admission_id      String              @db.Uuid
  document_type     String
  file_url          String?
  uploaded_at       DateTime?           @default(now()) @db.Timestamptz(6)
  admissions        admissions          @relation(fields: [admission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  student_documents student_documents[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_enquiries {
  id                String            @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id         String            @db.Uuid
  academic_year_id  String            @db.Uuid
  student_name      String
  grade_applied_for String
  parent_name       String
  parent_email      String
  parent_phone      String
  source            String
  status            String            @default("new")
  created_at        DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?         @default(now()) @db.Timestamptz(6)
  deleted_at        DateTime?         @db.Timestamptz(6)
  date_of_birth     DateTime?         @db.Date
  gender            String?
  current_school    String?
  address           String?
  remarks           String?
  academic_years    academic_years    @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools           schools           @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_leads   admission_leads[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_enrollment_logs {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  action                 String
  details                String?
  performed_by           String?                @db.Uuid
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  public_users?          @relation(fields: [performed_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_exam_results {
  id                                String                            @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  candidate_id                      String                            @db.Uuid
  subject_id                        String                            @db.Uuid
  marks_obtained                    Decimal                           @db.Decimal
  percentage                        Decimal                           @db.Decimal
  pass                              Boolean
  evaluator_id                      String?                           @db.Uuid
  created_at                        DateTime?                         @default(now()) @db.Timestamptz(6)
  updated_at                        DateTime?                         @default(now()) @db.Timestamptz(6)
  admission_exam_session_candidates admission_exam_session_candidates @relation(fields: [candidate_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                             public_users?                     @relation(fields: [evaluator_id], references: [id], onUpdate: NoAction)
  admission_exam_subjects           admission_exam_subjects           @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([candidate_id, subject_id], map: "unique_candidate_subject")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_exam_schedule {
  id                                String                              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id                       String                              @db.Uuid
  school_id                         String                              @db.Uuid
  academic_year_id                  String                              @db.Uuid
  room_name                         String
  invigilator_name                  String
  exam_date                         DateTime                            @db.Timestamptz(6)
  status                            String                              @default("SCHEDULED")
  created_at                        DateTime?                           @default(now()) @db.Timestamptz(6)
  updated_at                        DateTime?                           @default(now()) @db.Timestamptz(6)
  admission_assessment_policies     admission_assessment_policies?
  academic_years                    academic_years                      @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                           schools                             @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_exam_templates          admission_exam_templates            @relation(fields: [template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_exam_session_candidates admission_exam_session_candidates[]
  admission_hall_tickets            admission_hall_tickets[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_exam_session_candidates {
  id                            String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                    String                          @db.Uuid
  application_id                String                          @db.Uuid
  hall_ticket_number            String                          @unique
  seat_number                   String?
  attendance_status             String                          @default("PENDING")
  remarks                       String?
  created_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  admission_assessment_sessions admission_assessment_sessions[]
  admission_exam_results        admission_exam_results[]
  admission_applications        admission_applications          @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_exam_schedule       admission_exam_schedule         @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, application_id], map: "unique_session_candidate")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_exam_subjects {
  id                       String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id              String                   @db.Uuid
  subject_name             String
  max_marks                Int
  weightage                Int
  created_at               DateTime?                @default(now()) @db.Timestamptz(6)
  admission_exam_results   admission_exam_results[]
  admission_exam_templates admission_exam_templates @relation(fields: [template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([template_id, subject_name], map: "unique_subject_template")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_exam_templates {
  id                             String                           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name                           String
  grade                          String                           @unique
  duration                       Int
  total_marks                    Int
  passing_marks                  Int
  created_at                     DateTime?                        @default(now()) @db.Timestamptz(6)
  updated_at                     DateTime?                        @default(now()) @db.Timestamptz(6)
  version                        Int                              @default(1)
  is_locked                      Boolean?                         @default(false)
  evaluation_type                String?                          @default("MANUAL")
  admission_assessment_sections  admission_assessment_sections[]
  admission_assessment_snapshots admission_assessment_snapshots[]
  admission_exam_schedule        admission_exam_schedule[]
  admission_exam_subjects        admission_exam_subjects[]

  @@unique([grade, version], map: "unique_grade_version")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_fee_assignments {
  id                       String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id           String                   @db.Uuid
  component_id             String                   @db.Uuid
  amount                   Decimal                  @db.Decimal
  waived_amount            Decimal                  @default(0) @db.Decimal
  paid_amount              Decimal                  @default(0) @db.Decimal
  created_at               DateTime?                @default(now()) @db.Timestamptz(6)
  admission_applications   admission_applications   @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_fee_components admission_fee_components @relation(fields: [component_id], references: [id], onUpdate: NoAction)

  @@unique([application_id, component_id], map: "unique_app_fee_component")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_fee_components {
  id                        String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  structure_id              String                      @db.Uuid
  component_name            String
  amount                    Decimal                     @db.Decimal
  mandatory                 Boolean                     @default(true)
  created_at                DateTime?                   @default(now()) @db.Timestamptz(6)
  admission_fee_assignments admission_fee_assignments[]
  admission_fee_structures  admission_fee_structures    @relation(fields: [structure_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_fee_waivers     admission_fee_waivers[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_fee_structures {
  id                       String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                String                     @db.Uuid
  grade                    String
  academic_year_id         String                     @db.Uuid
  name                     String
  active                   Boolean                    @default(true)
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  admission_fee_components admission_fee_components[]
  academic_years           academic_years             @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                  schools                    @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, grade, academic_year_id], map: "unique_fee_structure")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_fee_waivers {
  id                       String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id           String                   @db.Uuid
  component_id             String                   @db.Uuid
  amount                   Decimal                  @db.Decimal
  remarks                  String
  approved_by              String?                  @db.Uuid
  created_at               DateTime?                @default(now()) @db.Timestamptz(6)
  admission_applications   admission_applications   @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                    public_users?            @relation(fields: [approved_by], references: [id], onUpdate: NoAction)
  admission_fee_components admission_fee_components @relation(fields: [component_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_fees {
  id                String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  admission_id      String     @db.Uuid
  fee_structure_id  String     @db.Uuid
  snapshot_name     String
  snapshot_amount   Decimal    @db.Decimal(10, 2)
  snapshot_category String
  is_mandatory      Boolean    @default(false)
  payment_status    String     @default("ENABLED")
  enabled_at        DateTime   @default(now()) @db.Timestamptz(6)
  enabled_by        String     @db.Uuid
  created_at        DateTime   @default(now()) @db.Timestamptz(6)
  admissions        admissions @relation(fields: [admission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users             auth_users @relation(fields: [enabled_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([admission_id, fee_structure_id])
  @@index([admission_id], map: "idx_admission_fees_admission_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_followups {
  id              String          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  lead_id         String          @db.Uuid
  scheduled_date  DateTime        @db.Timestamptz(6)
  completed_date  DateTime?       @db.Timestamptz(6)
  status          String          @default("scheduled")
  notes           String?
  created_by      String          @db.Uuid
  created_at      DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?       @default(now()) @db.Timestamptz(6)
  users           public_users    @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  admission_leads admission_leads @relation(fields: [lead_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([scheduled_date, status], map: "idx_followups_date")
  @@index([lead_id], map: "idx_followups_lead")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_hall_tickets {
  id                      String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id          String                  @db.Uuid
  exam_schedule_id        String                  @db.Uuid
  hall_ticket_number      String                  @unique
  exam_room               String
  reporting_time          DateTime                @db.Timestamptz(6)
  qr_code_path            String?
  created_at              DateTime?               @default(now()) @db.Timestamptz(6)
  admission_applications  admission_applications  @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_exam_schedule admission_exam_schedule @relation(fields: [exam_schedule_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_interview_criteria {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  code                       String                       @unique
  name                       String
  max_score                  Int                          @default(10)
  weightage                  Int                          @default(0)
  active                     Boolean                      @default(true)
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  admission_interview_scores admission_interview_scores[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_interview_panels {
  id                   String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  panel_name           String                 @unique
  members              String[]
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  admission_interviews admission_interviews[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_interview_scores {
  id                           String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  interview_id                 String                       @db.Uuid
  criterion_id                 String                       @db.Uuid
  score                        Decimal                      @db.Decimal
  remarks                      String?
  created_at                   DateTime?                    @default(now()) @db.Timestamptz(6)
  admission_interview_criteria admission_interview_criteria @relation(fields: [criterion_id], references: [id], onUpdate: NoAction)
  admission_interviews         admission_interviews         @relation(fields: [interview_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([interview_id, criterion_id], map: "unique_interview_criterion")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_interviews {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id             String                       @unique(map: "unique_app_interview") @db.Uuid
  panel_id                   String                       @db.Uuid
  interview_date             DateTime                     @db.Timestamptz(6)
  room_name                  String
  status                     String                       @default("SCHEDULED")
  remarks                    String?
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  admission_interview_scores admission_interview_scores[]
  admission_applications     admission_applications       @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_interview_panels admission_interview_panels   @relation(fields: [panel_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_leads {
  id                     String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  enquiry_id             String?                  @db.Uuid
  counselor_id           String?                  @db.Uuid
  status                 String                   @default("NEW")
  lost_reason            String?
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  deleted_at             DateTime?                @db.Timestamptz(6)
  admission_applications admission_applications[]
  admission_followups    admission_followups[]
  users                  public_users?            @relation(fields: [counselor_id], references: [id], onUpdate: NoAction)
  admission_enquiries    admission_enquiries?     @relation(fields: [enquiry_id], references: [id], onUpdate: NoAction)
  admission_visitors     admission_visitors[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_merit_components {
  id                    String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  rule_id               String                @db.Uuid
  component_name        String
  weight                Int
  active                Boolean               @default(true)
  created_at            DateTime?             @default(now()) @db.Timestamptz(6)
  admission_merit_rules admission_merit_rules @relation(fields: [rule_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([rule_id, component_name], map: "unique_rule_component")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_merit_results {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @unique @db.Uuid
  final_score            Decimal                @db.Decimal
  rank                   Int
  selection_status       String
  waitlist_priority      Int?
  waitlist_group         String?
  recommendation         String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_merit_rules {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                  String                       @db.Uuid
  academic_year_id           String                       @db.Uuid
  tie_breaker_rules          Json                         @default("[\"Exam Score\", \"Interview Score\", \"Age\", \"Application Date\"]")
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  admission_merit_components admission_merit_components[]
  academic_years             academic_years               @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                    schools                      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, academic_year_id], map: "unique_merit_rule")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_number_sequences {
  id            String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id     String    @unique @db.Uuid
  prefix        String    @default("SCH-2026-")
  suffix        String    @default("")
  current_value Int       @default(1)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  schools       schools   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_offer_letters {
  id                        String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id            String                    @unique @db.Uuid
  offer_number              String                    @unique
  template_id               String                    @db.Uuid
  issue_date                DateTime                  @db.Date
  acceptance_date           DateTime?                 @db.Date
  expiry_date               DateTime                  @db.Date
  status                    String                    @default("GENERATED")
  created_at                DateTime?                 @default(now()) @db.Timestamptz(6)
  updated_at                DateTime?                 @default(now()) @db.Timestamptz(6)
  admission_applications    admission_applications    @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_offer_templates admission_offer_templates @relation(fields: [template_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_offer_templates {
  id                      String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name                    String                    @unique
  content_body            String
  active                  Boolean                   @default(true)
  created_at              DateTime?                 @default(now()) @db.Timestamptz(6)
  admission_offer_letters admission_offer_letters[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_payment_receipts {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  payment_id         String             @unique @db.Uuid
  receipt_number     String             @unique
  issued_at          DateTime?          @default(now()) @db.Timestamptz(6)
  admission_payments admission_payments @relation(fields: [payment_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_payments {
  id                         String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id             String                      @db.Uuid
  amount                     Decimal                     @db.Decimal
  payment_mode               String
  transaction_number         String?
  gateway_reference          String?
  receipt_number             String                      @unique
  status                     String                      @default("PENDING")
  created_at                 DateTime?                   @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                   @default(now()) @db.Timestamptz(6)
  admission_payment_receipts admission_payment_receipts?
  admission_applications     admission_applications      @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_question_bank {
  id                                    String                                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                             String                                  @db.Uuid
  subject_id                            String                                  @db.Uuid
  question_text                         String
  question_type                         String
  difficulty                            String?                                 @default("MEDIUM")
  points                                Decimal                                 @default(1.00) @db.Decimal(5, 2)
  negative_marks                        Decimal?                                @default(0.00) @db.Decimal(5, 2)
  randomize_options                     Boolean?                                @default(true)
  created_at                            DateTime?                               @default(now()) @db.Timestamptz(6)
  updated_at                            DateTime?                               @default(now()) @db.Timestamptz(6)
  admission_assessment_question_mapping admission_assessment_question_mapping[]
  schools                               schools                                 @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_subjects                    admission_subjects                      @relation(fields: [subject_id], references: [id], onUpdate: NoAction)
  admission_question_options            admission_question_options[]

  @@index([subject_id], map: "idx_assessment_question_bank_subject")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_question_options {
  id                      String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  question_id             String                  @db.Uuid
  option_text             String
  is_correct              Boolean                 @default(false)
  created_at              DateTime?               @default(now()) @db.Timestamptz(6)
  admission_question_bank admission_question_bank @relation(fields: [question_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_subjects {
  id                      String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id               String                    @db.Uuid
  name                    String
  code                    String?                   @unique
  is_active               Boolean                   @default(true)
  created_at              DateTime?                 @default(now()) @db.Timestamptz(6)
  admission_question_bank admission_question_bank[]
  schools                 schools                   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_visitors {
  id                                           String           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                                    String           @db.Uuid
  visitor_name                                 String
  phone                                        String
  purpose                                      String
  time_in                                      DateTime         @default(now()) @db.Timestamptz(6)
  time_out                                     DateTime?        @db.Timestamptz(6)
  lead_id                                      String?          @db.Uuid
  created_by                                   String           @db.Uuid
  created_at                                   DateTime?        @default(now()) @db.Timestamptz(6)
  counselor_id                                 String?          @db.Uuid
  remarks                                      String?
  visit_type                                   String?
  visit_outcome                                String?
  users_admission_visitors_counselor_idTousers public_users?    @relation("admission_visitors_counselor_idTousers", fields: [counselor_id], references: [id], onUpdate: NoAction)
  users_admission_visitors_created_byTousers   public_users     @relation("admission_visitors_created_byTousers", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  admission_leads                              admission_leads? @relation(fields: [lead_id], references: [id], onUpdate: NoAction)
  schools                                      schools          @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([lead_id], map: "idx_visitors_lead")
  @@index([time_in, visitor_name], map: "idx_visitors_lookup")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admission_waitlist_promotions {
  id                                                                                               String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_application_id                                                                              String                 @db.Uuid
  to_application_id                                                                                String                 @db.Uuid
  promoted_at                                                                                      DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications_admission_waitlist_promotions_from_application_idToadmission_applications admission_applications @relation("admission_waitlist_promotions_from_application_idToadmission_applications", fields: [from_application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_applications_admission_waitlist_promotions_to_application_idToadmission_applications   admission_applications @relation("admission_waitlist_promotions_to_application_idToadmission_applications", fields: [to_application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model admissions {
  id                   String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id            String                 @db.Uuid
  academic_year_id     String                 @db.Uuid
  applicant_user_id    String?                @db.Uuid
  student_name         String
  date_of_birth        DateTime               @db.Date
  gender               String?
  grade_applied_for    String
  status               String?                @default("draft")
  submitted_at         DateTime?              @db.Timestamptz(6)
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?              @default(now()) @db.Timestamptz(6)
  parent_name          String?
  parent_email         String?
  parent_phone         String?
  address              String?
  previous_school      String?
  last_grade_completed String?
  remark_by_officer    String?
  remark_by_hoi        String?
  recommended_at       DateTime?              @db.Timestamptz(6)
  approved_at          DateTime?              @db.Timestamptz(6)
  rejected_at          DateTime?              @db.Timestamptz(6)
  rejection_reason     String?
  parent_password      String?
  payment_enabled      Boolean?               @default(false)
  payment_amount       Decimal?               @db.Decimal(10, 2)
  payment_mode         String?
  payment_reference    String?
  payment_proof        String?
  payment_date         DateTime?              @db.Timestamptz(6)
  payment_verified     Boolean?               @default(false)
  remark_by_finance    String?
  mother_name          String?
  mother_email         String?
  mother_phone         String?
  father_name          String?
  father_email         String?
  father_phone         String?
  deleted_at           DateTime?              @db.Timestamptz(6)
  admission_audit_logs admission_audit_logs[]
  admission_documents  admission_documents[]
  admission_fees       admission_fees[]
  academic_years       academic_years         @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                public_users?          @relation(fields: [applicant_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools              schools                @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students             students?

  @@index([applicant_user_id], map: "idx_admissions_applicant_user_id")
  @@index([deleted_at], map: "idx_admissions_deleted_at")
  @@index([school_id], map: "idx_admissions_school_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model application_declarations {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  agreed_to_terms        Boolean                @default(false)
  parent_signature       String?
  date_signed            DateTime?              @db.Date
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model application_documents {
  id                                             String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id                                 String                  @db.Uuid
  document_type_id                               String                  @db.Uuid
  original_filename                              String
  stored_filename                                String
  storage_provider                               String                  @default("Supabase")
  storage_bucket                                 String
  storage_path                                   String
  mime_type                                      String
  extension                                      String
  file_size                                      Int
  checksum                                       String                  @unique
  version                                        Int                     @default(1)
  status                                         String                  @default("UPLOADED")
  uploaded_by                                    String?                 @db.Uuid
  verified_by                                    String?                 @db.Uuid
  uploaded_at                                    DateTime?               @default(now()) @db.Timestamptz(6)
  verified_at                                    DateTime?               @db.Timestamptz(6)
  deleted_at                                     DateTime?               @db.Timestamptz(6)
  created_at                                     DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at                                     DateTime?               @default(now()) @db.Timestamptz(6)
  admission_applications                         admission_applications  @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  document_types                                 document_types          @relation(fields: [document_type_id], references: [id], onUpdate: NoAction)
  users_application_documents_uploaded_byTousers public_users?           @relation("application_documents_uploaded_byTousers", fields: [uploaded_by], references: [id], onUpdate: NoAction)
  users_application_documents_verified_byTousers public_users?           @relation("application_documents_verified_byTousers", fields: [verified_by], references: [id], onUpdate: NoAction)
  document_comments                              document_comments[]
  document_verification                          document_verification[]
  document_versions                              document_versions[]

  @@index([application_id], map: "idx_documents_app")
  @@index([status], map: "idx_documents_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model application_parents {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  father_name            String?
  mother_name            String?
  guardian_name          String?
  guardian_relation      String?
  father_occupation      String?
  mother_occupation      String?
  guardian_occupation    String?
  father_income          Decimal?               @db.Decimal
  mother_income          Decimal?               @db.Decimal
  guardian_income        Decimal?               @db.Decimal
  father_education       String?
  mother_education       String?
  guardian_education     String?
  father_phone           String?
  mother_phone           String?
  guardian_phone         String?
  father_email           String?
  mother_email           String?
  guardian_email         String?
  father_address         String?
  mother_address         String?
  guardian_address       String?
  emergency_contact      String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model application_preferences {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  need_transport         Boolean?               @default(false)
  route_preference       String?
  pickup_point           String?
  need_hostel            Boolean?               @default(false)
  room_preference        String?
  special_requirements   String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model application_previous_education {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  school_name            String
  board                  String?
  medium                 String?
  last_class             String?
  percentage             Decimal?               @db.Decimal
  subjects               String?
  tc_number              String?
  reason_leaving         String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model application_profiles {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  date_of_birth          DateTime               @db.Date
  gender                 String
  blood_group            String?
  nationality            String?
  religion               String?
  category               String?
  aadhaar                String?
  photo_url              String?
  allergies              String?
  medical_conditions     String?
  emergency_notes        String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model application_workflow {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  action                 String
  from_status            String?
  to_status              String
  performed_by           String?                @db.Uuid
  notes                  String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  public_users?          @relation(fields: [performed_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model approval_history {
  id                  String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id           String              @db.Uuid
  publication_id      String              @db.Uuid
  stage               String
  approved_by         String?             @db.Uuid
  status              String?             @default("APPROVED")
  comments            String?
  created_at          DateTime            @default(now()) @db.Timestamptz(6)
  users               public_users?       @relation(fields: [approved_by], references: [id], onUpdate: NoAction)
  result_publications result_publications @relation(fields: [publication_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools             schools             @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model approval_requests {
  id                                              String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workflow_run_id                                 String?        @db.Uuid
  node_id                                         String
  assigned_role                                   String
  assigned_user_id                                String?        @db.Uuid
  status                                          String         @default("pending")
  remarks                                         String?
  created_at                                      DateTime?      @default(now()) @db.Timestamptz(6)
  decided_at                                      DateTime?      @db.Timestamptz(6)
  decided_by                                      String?        @db.Uuid
  users_approval_requests_assigned_user_idTousers public_users?  @relation("approval_requests_assigned_user_idTousers", fields: [assigned_user_id], references: [id], onUpdate: NoAction)
  users_approval_requests_decided_byTousers       public_users?  @relation("approval_requests_decided_byTousers", fields: [decided_by], references: [id], onUpdate: NoAction)
  workflow_runs                                   workflow_runs? @relation(fields: [workflow_run_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([workflow_run_id, status], map: "idx_approval_requests_run")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_accreditation_reports {
  id                      String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id               String        @db.Uuid
  report_type             String
  generated_by            String?       @db.Uuid
  attainment_metrics_json Json          @default("{}")
  created_at              DateTime      @default(now()) @db.Timestamptz(6)
  users                   public_users? @relation(fields: [generated_by], references: [id], onUpdate: NoAction)
  schools                 schools       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_ai_evaluation_queue {
  id                   String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  attempt_id           String   @db.Uuid
  question_snapshot_id String   @db.Uuid
  model_name           String
  prompt_version       String
  confidence_score     Decimal  @default(1.00) @db.Decimal(4, 2)
  status               String   @default("PENDING")
  created_at           DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_analytics_snapshots {
  id               String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String   @db.Uuid
  snapshot_date    DateTime @default(dbgenerated("CURRENT_DATE")) @db.Date
  snapshot_type    String
  academic_year_id String   @db.Uuid
  payload          Json     @default("{}")
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  schools          schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_assets {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                  String                       @db.Uuid
  file_name                  String
  file_path                  String
  mime_type                  String
  file_size                  Int
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  schools                    schools                      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_question_assets assessment_question_assets[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_attempts {
  id                        String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                String                      @db.Uuid
  student_id                String                      @db.Uuid
  status                    String                      @default("IN_PROGRESS")
  browser_state             Json                        @default("{}")
  started_at                DateTime                    @default(now()) @db.Timestamptz(6)
  submitted_at              DateTime?                   @db.Timestamptz(6)
  last_heartbeat_at         DateTime                    @default(now()) @db.Timestamptz(6)
  autosaved_answers         Json                        @default("{}")
  created_at                DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  assessment_sessions       assessment_sessions         @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students                  students                    @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_marks          assessment_marks[]
  assessment_sync_conflicts assessment_sync_conflicts[]
  exam_violation_log        exam_violation_log[]

  @@unique([session_id, student_id], map: "unique_student_session_attempt")
  @@index([student_id], map: "idx_assessment_attempts_student")
  @@index([student_id, session_id], map: "idx_assessment_attempts_student_session")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_backlog_subjects {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String   @db.Uuid
  subject_id String   @db.Uuid
  status     String
  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model assessment_benchmark_reports {
  id                 String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String   @db.Uuid
  academic_year_id   String   @db.Uuid
  comparison_year_id String   @db.Uuid
  gpa_delta          Decimal  @default(0.00) @db.Decimal(4, 2)
  improvement_areas  String[]
  created_at         DateTime @default(now()) @db.Timestamptz(6)
  schools            schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_bloom_analytics {
  id                String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id         String   @db.Uuid
  bloom_level       String
  questions_count   Int      @default(0)
  average_marks_pct Decimal  @default(0.00) @db.Decimal(5, 2)
  calculated_at     DateTime @default(now()) @db.Timestamptz(6)
  schools           schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_bloom_rules {
  id           String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  blueprint_id String  @db.Uuid
  bloom_level  String
  percentage   Decimal @db.Decimal(5, 2)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_difficulty_rules {
  id               String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  blueprint_id     String  @db.Uuid
  difficulty_level String
  percentage       Decimal @db.Decimal(5, 2)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_outcomes {
  id               String @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  blueprint_id     String @db.Uuid
  outcome_code     String
  target_questions Int

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_randomization_rules {
  id                    String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  blueprint_id          String  @db.Uuid
  shuffle_questions     Boolean @default(true)
  shuffle_options       Boolean @default(true)
  alternative_pool_size Int     @default(0)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_rules {
  id                            String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  section_id                    String                        @db.Uuid
  filter_field                  String
  filter_value                  String
  match_operator                String                        @default("eq")
  assessment_blueprint_sections assessment_blueprint_sections @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([section_id], map: "idx_assessment_blueprint_rules_sec")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_sections {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  blueprint_id               String                       @db.Uuid
  section_name               String
  description                String?
  points_per_question        Decimal                      @default(1.00) @db.Decimal(5, 2)
  negative_marks             Decimal?                     @default(0.00) @db.Decimal(5, 2)
  total_questions            Int
  sort_order                 Int
  assessment_blueprint_rules assessment_blueprint_rules[]

  @@index([blueprint_id], map: "idx_assessment_blueprint_sections_bp")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_validation_logs {
  id                String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  blueprint_id      String        @db.Uuid
  validation_status String
  errors            Json          @default("[]")
  warnings          Json          @default("[]")
  validated_by      String?       @db.Uuid
  validated_at      DateTime      @default(now()) @db.Timestamptz(6)
  users             public_users? @relation(fields: [validated_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_blueprint_versions {
  id              String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  blueprint_id    String   @db.Uuid
  version         Int
  schema_snapshot Json
  created_at      DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_certificate_queue {
  id                               String                           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id                       String                           @db.Uuid
  template_id                      String                           @db.Uuid
  status                           String                           @default("QUEUED")
  error_message                    String?
  pdf_url                          String?
  created_at                       DateTime                         @default(now()) @db.Timestamptz(6)
  assessment_certificate_templates assessment_certificate_templates @relation(fields: [template_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_certificate_templates {
  id                           String                         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                    String                         @db.Uuid
  name                         String
  styles_css                   String?
  layout_json                  Json                           @default("{}")
  created_at                   DateTime                       @default(now()) @db.Timestamptz(6)
  assessment_certificate_queue assessment_certificate_queue[]
  schools                      schools                        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_co_attainment {
  id                    String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id             String   @db.Uuid
  subject_id            String   @db.Uuid
  co_code               String
  attainment_target_pct Decimal  @default(70.00) @db.Decimal(5, 2)
  actual_attainment_pct Decimal  @default(0.00) @db.Decimal(5, 2)
  status                String   @default("NOT_MET")
  calculated_at         DateTime @default(now()) @db.Timestamptz(6)
  schools               schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_configurations {
  id                       String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                String   @unique @db.Uuid
  max_upload_size_mb       Int?     @default(10)
  autosave_interval_secs   Int?     @default(10)
  default_heartbeat_secs   Int?     @default(30)
  timezone                 String   @default("UTC")
  grading_scale            Json     @default("[]")
  retention_telemetry_days Int?     @default(90)
  retention_attempts_years Int?     @default(7)
  created_at               DateTime @default(now()) @db.Timestamptz(6)
  updated_at               DateTime @default(now()) @db.Timestamptz(6)
  settings                 Json     @default("{}")
  schools                  schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([school_id], map: "idx_assessment_config_school")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_dashboard_cache {
  id            String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id     String   @db.Uuid
  role_key      String
  cache_payload Json     @default("{}")
  expires_at    DateTime @db.Timestamptz(6)
  updated_at    DateTime @default(now()) @db.Timestamptz(6)
  schools       schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model assessment_department_statistics {
  id                 String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  department_id      String   @db.Uuid
  academic_year_id   String   @db.Uuid
  average_gpa        Decimal  @default(0.00) @db.Decimal(4, 2)
  retention_rate_pct Decimal  @default(100.00) @db.Decimal(5, 2)
  backlog_ratio_pct  Decimal  @default(0.00) @db.Decimal(5, 2)
  updated_at         DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_evaluation_annotations {
  id                              String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  question_evaluation_id          String                          @db.Uuid
  type                            String
  coordinates                     Json                            @default("{}")
  comment_text                    String?
  annotated_by                    String?                         @db.Uuid
  created_at                      DateTime                        @default(now()) @db.Timestamptz(6)
  users                           public_users?                   @relation(fields: [annotated_by], references: [id], onUpdate: NoAction)
  assessment_question_evaluations assessment_question_evaluations @relation(fields: [question_evaluation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_evaluation_assignments {
  id                             String                           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                      String                           @db.Uuid
  evaluator_id                   String                           @db.Uuid
  subject_id                     String                           @db.Uuid
  published_paper_id             String                           @db.Uuid
  workload_limit                 Int                              @default(50)
  priority                       String                           @default("MEDIUM")
  created_at                     DateTime                         @default(now()) @db.Timestamptz(6)
  users                          public_users                     @relation(fields: [evaluator_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                        schools                          @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects                       subjects                         @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_evaluation_sessions assessment_evaluation_sessions[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_evaluation_locks {
  id                             String                         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  evaluation_session_id          String                         @db.Uuid
  evaluator_id                   String                         @db.Uuid
  locked_at                      DateTime                       @default(now()) @db.Timestamptz(6)
  expires_at                     DateTime                       @db.Timestamptz(6)
  device_id                      String?
  assessment_evaluation_sessions assessment_evaluation_sessions @relation(fields: [evaluation_session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                          public_users                   @relation(fields: [evaluator_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_evaluation_logs {
  id                             String                         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                     String                         @db.Uuid
  action                         String
  before_state                   Json                           @default("{}")
  after_state                    Json                           @default("{}")
  evaluator_id                   String?                        @db.Uuid
  logged_at                      DateTime                       @default(now()) @db.Timestamptz(6)
  users                          public_users?                  @relation(fields: [evaluator_id], references: [id], onUpdate: NoAction)
  assessment_evaluation_sessions assessment_evaluation_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_evaluation_sessions {
  id                                String                             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                         String                             @db.Uuid
  assignment_id                     String?                            @db.Uuid
  published_paper_id                String                             @db.Uuid
  attempt_id                        String                             @db.Uuid
  evaluator_id                      String?                            @db.Uuid
  status                            String                             @default("DRAFT")
  anonymous_mode                    Boolean                            @default(true)
  moderation_required               Boolean                            @default(false)
  assigned_at                       DateTime                           @default(now()) @db.Timestamptz(6)
  completed_at                      DateTime?                          @db.Timestamptz(6)
  assessment_evaluation_locks       assessment_evaluation_locks[]
  assessment_evaluation_logs        assessment_evaluation_logs[]
  assessment_evaluation_assignments assessment_evaluation_assignments? @relation(fields: [assignment_id], references: [id], onUpdate: NoAction)
  users                             public_users?                      @relation(fields: [evaluator_id], references: [id], onUpdate: NoAction)
  schools                           schools                            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_moderation_queue       assessment_moderation_queue[]
  assessment_question_evaluations   assessment_question_evaluations[]

  @@schema("public")
}

model assessment_exam_statistics {
  id               String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  exam_id          String   @db.Uuid
  total_candidates Int      @default(0)
  absent_count     Int      @default(0)
  passed_count     Int      @default(0)
  failed_count     Int      @default(0)
  class_average    Decimal  @default(0.00) @db.Decimal(6, 2)
  class_median     Decimal  @default(0.00) @db.Decimal(6, 2)
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_folders {
  id                       String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                String               @db.Uuid
  parent_id                String?              @db.Uuid
  name                     String
  is_deleted               Boolean              @default(false)
  deleted_at               DateTime?            @db.Timestamptz(6)
  deleted_by               String?              @db.Uuid
  created_at               DateTime             @default(now()) @db.Timestamptz(6)
  updated_at               DateTime             @default(now()) @db.Timestamptz(6)
  users                    public_users?        @relation(fields: [deleted_by], references: [id], onUpdate: NoAction)
  assessment_folders       assessment_folders?  @relation("assessment_foldersToassessment_folders", fields: [parent_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  other_assessment_folders assessment_folders[] @relation("assessment_foldersToassessment_folders")
  schools                  schools              @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generated_exports {
  id                          String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  paper_id                    String                      @db.Uuid
  format                      String
  type                        String
  file_path                   String
  generated_by                String?                     @db.Uuid
  generated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  users                       public_users?               @relation(fields: [generated_by], references: [id], onUpdate: NoAction)
  assessment_generated_papers assessment_generated_papers @relation(fields: [paper_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generated_papers {
  id                                   String                                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                            String                                 @db.Uuid
  subject_id                           String                                 @db.Uuid
  blueprint_id                         String?                                @db.Uuid
  template_id                          String?                                @db.Uuid
  name                                 String
  description                          String?
  total_marks                          Decimal                                @default(100.00) @db.Decimal(6, 2)
  status                               String                                 @default("DRAFT")
  version                              Int                                    @default(1)
  is_deleted                           Boolean                                @default(false)
  created_by                           String?                                @db.Uuid
  created_at                           DateTime                               @default(now()) @db.Timestamptz(6)
  updated_at                           DateTime                               @default(now()) @db.Timestamptz(6)
  assessment_generated_exports         assessment_generated_exports[]
  users                                public_users?                          @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  schools                              schools                                @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects                             subjects                               @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_generated_sections        assessment_generated_sections[]
  assessment_generated_statistics      assessment_generated_statistics[]
  assessment_generated_validation_logs assessment_generated_validation_logs[]

  @@index([school_id], map: "idx_gen_papers_school")
  @@index([subject_id], map: "idx_gen_papers_subject")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generated_questions {
  id                            String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  section_id                    String                        @db.Uuid
  question_id                   String                        @db.Uuid
  sort_order                    Int
  assessment_generated_sections assessment_generated_sections @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generated_sections {
  id                             String                           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  paper_id                       String                           @db.Uuid
  section_name                   String
  description                    String?
  points_per_question            Decimal                          @default(1.00) @db.Decimal(6, 2)
  negative_marks                 Decimal                          @default(0.00) @db.Decimal(6, 2)
  total_questions                Int
  sort_order                     Int
  assessment_generated_questions assessment_generated_questions[]
  assessment_generated_papers    assessment_generated_papers      @relation(fields: [paper_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generated_statistics {
  id                          String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  paper_id                    String                      @db.Uuid
  generation_duration_ms      Int                         @default(0)
  blueprint_compliance_pct    Decimal                     @default(100.00) @db.Decimal(5, 2)
  question_reuse_pct          Decimal                     @default(0.00) @db.Decimal(5, 2)
  difficulty_compliance_pct   Decimal                     @default(100.00) @db.Decimal(5, 2)
  bloom_compliance_pct        Decimal                     @default(100.00) @db.Decimal(5, 2)
  outcome_compliance_pct      Decimal                     @default(100.00) @db.Decimal(5, 2)
  assessment_generated_papers assessment_generated_papers @relation(fields: [paper_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generated_validation_logs {
  id                          String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  paper_id                    String                      @db.Uuid
  validation_status           String
  errors                      Json                        @default("[]")
  warnings                    Json                        @default("[]")
  validated_by                String?                     @db.Uuid
  validated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  assessment_generated_papers assessment_generated_papers @relation(fields: [paper_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                       public_users?               @relation(fields: [validated_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generation_jobs {
  id            String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id     String        @db.Uuid
  blueprint_id  String        @db.Uuid
  template_id   String        @db.Uuid
  status        String        @default("PENDING")
  logs          Json          @default("[]")
  error_message String?
  created_by    String?       @db.Uuid
  created_at    DateTime      @default(now()) @db.Timestamptz(6)
  updated_at    DateTime      @default(now()) @db.Timestamptz(6)
  users         public_users? @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  schools       schools       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([school_id], map: "idx_gen_jobs_school")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_generation_locks {
  id            String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  resource_type String
  resource_id   String        @db.Uuid
  locked_by     String?       @db.Uuid
  locked_at     DateTime      @default(now()) @db.Timestamptz(6)
  expires_at    DateTime      @db.Timestamptz(6)
  users         public_users? @relation(fields: [locked_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_grace_rules {
  id                String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id         String   @db.Uuid
  subject_id        String   @db.Uuid
  policy_name       String
  grace_marks_limit Decimal  @default(5.00) @db.Decimal(4, 2)
  is_active         Boolean  @default(true)
  schools           schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects          subjects @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_grade_calculations {
  id                       String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                String                     @db.Uuid
  attempt_id               String                     @db.Uuid
  raw_marks                Decimal                    @default(0.00) @db.Decimal(6, 2)
  scaled_marks             Decimal                    @default(0.00) @db.Decimal(6, 2)
  grace_marks              Decimal                    @default(0.00) @db.Decimal(6, 2)
  final_marks              Decimal                    @default(0.00) @db.Decimal(6, 2)
  grade_label              String
  grade_point              Decimal                    @default(0.00) @db.Decimal(4, 2)
  credits                  Int                        @default(0)
  calculated_at            DateTime                   @default(now()) @db.Timestamptz(6)
  schools                  schools                    @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_grade_history assessment_grade_history[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_grade_cards {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_result_id          String                     @db.Uuid
  issue_number               String                     @unique
  issued_by                  String?                    @db.Uuid
  issued_at                  DateTime                   @default(now()) @db.Timestamptz(6)
  users                      public_users?              @relation(fields: [issued_by], references: [id], onUpdate: NoAction)
  assessment_student_results assessment_student_results @relation(fields: [student_result_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_grade_history {
  id                            String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  calculation_id                String                        @db.Uuid
  raw_marks                     Decimal                       @db.Decimal(6, 2)
  final_marks                   Decimal                       @db.Decimal(6, 2)
  grade_label                   String
  changed_by                    String?                       @db.Uuid
  change_reason                 String?
  changed_at                    DateTime                      @default(now()) @db.Timestamptz(6)
  assessment_grade_calculations assessment_grade_calculations @relation(fields: [calculation_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                         public_users?                 @relation(fields: [changed_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_grading_scales {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id    String   @db.Uuid
  name         String
  scale_config Json     @default("[]")
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  schools      schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, name], map: "unique_school_assessment_grading_scale_name")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_import_logs {
  id                  String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id           String               @db.Uuid
  session_id          String?              @db.Uuid
  imported_by         String?              @db.Uuid
  file_url            String
  records_count       Int                  @default(0)
  success_count       Int                  @default(0)
  failure_count       Int                  @default(0)
  status              String               @default("PENDING")
  error_details       String?
  created_at          DateTime             @default(now()) @db.Timestamptz(6)
  users               public_users?        @relation(fields: [imported_by], references: [id], onUpdate: NoAction)
  schools             schools              @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_sessions assessment_sessions? @relation(fields: [session_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model assessment_learning_gap_analysis {
  id                         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id                 String   @db.Uuid
  subject_id                 String   @db.Uuid
  gap_description            String
  remedial_class_recommended Boolean  @default(false)
  remedial_status            String   @default("PENDING")
  created_at                 DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_marks {
  id                  String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  attempt_id          String              @db.Uuid
  question_id         String              @db.Uuid
  student_answer      Json                @default("{}")
  auto_score          Decimal?            @default(0.00) @db.Decimal(5, 2)
  manual_score        Decimal?            @db.Decimal(5, 2)
  evaluator_id        String?             @db.Uuid
  evaluation_status   String              @default("PENDING")
  remarks             String?
  created_at          DateTime            @default(now()) @db.Timestamptz(6)
  updated_at          DateTime            @default(now()) @db.Timestamptz(6)
  assessment_attempts assessment_attempts @relation(fields: [attempt_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users               public_users?       @relation(fields: [evaluator_id], references: [id], onUpdate: NoAction)

  @@unique([attempt_id, question_id], map: "unique_attempt_question_marks")
  @@index([attempt_id], map: "idx_assessment_marks_attempt")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_moderation_queue {
  id                             String                         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                     String                         @db.Uuid
  first_evaluator_marks          Decimal                        @db.Decimal(6, 2)
  second_evaluator_marks         Decimal                        @db.Decimal(6, 2)
  variance_pct                   Decimal                        @default(0.00) @db.Decimal(5, 2)
  moderator_id                   String?                        @db.Uuid
  moderator_marks                Decimal?                       @db.Decimal(6, 2)
  status                         String                         @default("PENDING")
  created_at                     DateTime                       @default(now()) @db.Timestamptz(6)
  users                          public_users?                  @relation(fields: [moderator_id], references: [id], onUpdate: NoAction)
  assessment_evaluation_sessions assessment_evaluation_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_official_gradecards {
  id                          String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  official_result_id          String                      @db.Uuid
  gradecard_pdf_url           String
  checksum                    String
  issued_at                   DateTime                    @default(now()) @db.Timestamptz(6)
  assessment_official_results assessment_official_results @relation(fields: [official_result_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_official_results {
  id                                  String                                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                           String                                @db.Uuid
  student_id                          String                                @db.Uuid
  academic_year_id                    String                                @db.Uuid
  term_id                             String                                @db.Uuid
  final_percentage                    Decimal                               @db.Decimal(5, 2)
  gpa                                 Decimal                               @db.Decimal(4, 2)
  cgpa                                Decimal                               @db.Decimal(4, 2)
  total_credits                       Int
  signed_hash                         String
  archived_at                         DateTime                              @default(now()) @db.Timestamptz(6)
  assessment_official_gradecards      assessment_official_gradecards[]
  schools                             schools                               @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_official_subject_results assessment_official_subject_results[]

  @@index([school_id], map: "idx_off_res_school")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_official_subject_results {
  id                          String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  official_result_id          String                      @db.Uuid
  subject_id                  String                      @db.Uuid
  awarded_marks               Decimal                     @db.Decimal(6, 2)
  maximum_marks               Decimal                     @db.Decimal(6, 2)
  grade_label                 String
  grade_point                 Decimal                     @db.Decimal(4, 2)
  assessment_official_results assessment_official_results @relation(fields: [official_result_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_official_transcripts {
  id                 String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id         String   @db.Uuid
  transcript_pdf_url String
  checksum           String
  issued_at          DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_paper_packages {
  id                          String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  published_paper_id          String                      @db.Uuid
  candidate_pdf               String?
  moderator_pdf               String?
  answer_key_pdf              String?
  encrypted_package           String?
  checksum                    String
  metadata                    Json                        @default("{}")
  created_at                  DateTime                    @default(now()) @db.Timestamptz(6)
  assessment_published_papers assessment_published_papers @relation(fields: [published_paper_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_po_attainment {
  id               String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String   @db.Uuid
  po_code          String
  attainment_score Decimal  @default(0.00) @db.Decimal(4, 2)
  target_score     Decimal  @default(3.00) @db.Decimal(4, 2)
  calculated_at    DateTime @default(now()) @db.Timestamptz(6)
  schools          schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model assessment_prediction_models {
  id             String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  model_name     String
  version_label  String
  accuracy_score Decimal @default(1.00) @db.Decimal(4, 2)
  features_json  Json    @default("[]")

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_promotion_decisions {
  id               String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id       String        @db.Uuid
  academic_year_id String        @db.Uuid
  decision         String
  remarks          String?
  decided_by       String?       @db.Uuid
  created_at       DateTime      @default(now()) @db.Timestamptz(6)
  users            public_users? @relation(fields: [decided_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_published_papers {
  id                            String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                     String                          @db.Uuid
  template_version_id           String?                         @db.Uuid
  subject_id                    String                          @db.Uuid
  paper_name                    String
  total_marks                   Decimal                         @db.Decimal(6, 2)
  questions_structure           Json                            @default("[]")
  created_by                    String?                         @db.Uuid
  created_at                    DateTime                        @default(now()) @db.Timestamptz(6)
  assessment_paper_packages     assessment_paper_packages[]
  users                         public_users?                   @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  schools                       schools                         @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects                      subjects                        @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_template_versions  assessment_template_versions?   @relation(fields: [template_version_id], references: [id], onUpdate: NoAction)
  assessment_published_sections assessment_published_sections[]
  assessment_sessions           assessment_sessions[]

  @@index([school_id], map: "idx_assessment_pub_papers_school")
  @@index([school_id], map: "idx_pub_papers_school")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_published_questions {
  id                            String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  published_section_id          String                        @db.Uuid
  question_snapshot             Json
  options_snapshot              Json                          @default("[]")
  asset_snapshot                Json                          @default("[]")
  answer_key_snapshot           Json                          @default("[]")
  question_order                Int
  option_order                  Json                          @default("[]")
  section_order                 Int
  assessment_published_sections assessment_published_sections @relation(fields: [published_section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_published_sections {
  id                             String                           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  published_paper_id             String                           @db.Uuid
  section_name                   String
  description                    String?
  points_per_question            Decimal                          @default(1.00) @db.Decimal(6, 2)
  negative_marks                 Decimal                          @default(0.00) @db.Decimal(6, 2)
  total_questions                Int
  sort_order                     Int
  assessment_published_questions assessment_published_questions[]
  assessment_published_papers    assessment_published_papers      @relation(fields: [published_paper_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_quality_audits {
  id                    String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id             String        @db.Uuid
  audited_by            String?       @db.Uuid
  audit_date            DateTime      @default(dbgenerated("CURRENT_DATE")) @db.Date
  checklist_status_json Json          @default("{}")
  is_compliant          Boolean       @default(true)
  remarks               String?
  users                 public_users? @relation(fields: [audited_by], references: [id], onUpdate: NoAction)
  schools               schools       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_question_assets {
  question_id       String            @db.Uuid
  asset_id          String            @db.Uuid
  assessment_assets assessment_assets @relation(fields: [asset_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([question_id, asset_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_question_evaluations {
  id                                String                              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                        String                              @db.Uuid
  question_snapshot_id              String                              @db.Uuid
  awarded_marks                     Decimal                             @default(0.00) @db.Decimal(6, 2)
  maximum_marks                     Decimal                             @db.Decimal(6, 2)
  remarks                           String?
  evaluator_id                      String?                             @db.Uuid
  evaluated_at                      DateTime                            @default(now()) @db.Timestamptz(6)
  assessment_evaluation_annotations assessment_evaluation_annotations[]
  users                             public_users?                       @relation(fields: [evaluator_id], references: [id], onUpdate: NoAction)
  assessment_evaluation_sessions    assessment_evaluation_sessions      @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_question_options {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  question_id String    @db.Uuid
  option_text String
  is_correct  Boolean   @default(false)
  is_deleted  Boolean   @default(false)
  deleted_at  DateTime? @db.Timestamptz(6)

  @@index([question_id], map: "idx_assessment_q_options_q")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_question_statistics {
  id                         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  question_snapshot_id       String   @db.Uuid
  facility_value             Decimal  @default(0.00) @db.Decimal(4, 2)
  difficulty_index           Decimal  @default(0.00) @db.Decimal(4, 2)
  discrimination_index       Decimal  @default(0.00) @db.Decimal(4, 2)
  skipped_pct                Decimal  @default(0.00) @db.Decimal(5, 2)
  average_time_spent_seconds Int      @default(0)
  median_marks               Decimal  @default(0.00) @db.Decimal(6, 2)
  standard_deviation         Decimal  @default(0.00) @db.Decimal(6, 2)
  bloom_attainment_pct       Decimal  @default(100.00) @db.Decimal(5, 2)
  outcome_attainment_pct     Decimal  @default(100.00) @db.Decimal(5, 2)
  calculated_at              DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_rankings {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  student_id                 String                     @db.Uuid
  cgpa                       Decimal                    @db.Decimal(4, 2)
  merit_rank                 Int
  created_at                 DateTime                   @default(now()) @db.Timestamptz(6)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model assessment_report_exports {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id   String   @db.Uuid
  format      String
  report_type String
  file_path   String
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  schools     schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_approval_workflow {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  approved_by                String?                    @db.Uuid
  role_level                 String
  decision                   String
  comments                   String?
  signed_at                  DateTime                   @default(now()) @db.Timestamptz(6)
  users                      public_users?              @relation(fields: [approved_by], references: [id], onUpdate: NoAction)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_audit_logs {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  action                     String
  performed_by               String?                    @db.Uuid
  performed_at               DateTime                   @default(now()) @db.Timestamptz(6)
  metadata                   Json                       @default("{}")
  users                      public_users?              @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_exports {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  format                     String
  type                       String
  file_path                  String
  checksum                   String
  created_at                 DateTime                   @default(now()) @db.Timestamptz(6)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_freeze_logs {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  frozen_by                  String?                    @db.Uuid
  frozen_at                  DateTime                   @default(now()) @db.Timestamptz(6)
  users                      public_users?              @relation(fields: [frozen_by], references: [id], onUpdate: NoAction)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_notifications {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String   @db.Uuid
  medium     String
  message    String
  status     String   @default("PENDING")
  sent_at    DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_publications {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  target_portal              String                     @default("STUDENT_PORTAL")
  published_by               String?                    @db.Uuid
  published_at               DateTime                   @default(now()) @db.Timestamptz(6)
  users                      public_users?              @relation(fields: [published_by], references: [id], onUpdate: NoAction)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_sessions {
  id                                  String                                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                           String                                @db.Uuid
  academic_year_id                    String                                @db.Uuid
  term_id                             String                                @db.Uuid
  status                              String                                @default("DRAFT")
  created_by                          String?                               @db.Uuid
  created_at                          DateTime                              @default(now()) @db.Timestamptz(6)
  updated_at                          DateTime                              @default(now()) @db.Timestamptz(6)
  assessment_rankings                 assessment_rankings[]
  assessment_result_approval_workflow assessment_result_approval_workflow[]
  assessment_result_audit_logs        assessment_result_audit_logs[]
  assessment_result_exports           assessment_result_exports[]
  assessment_result_freeze_logs       assessment_result_freeze_logs[]
  assessment_result_publications      assessment_result_publications[]
  users                               public_users?                         @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  schools                             schools                               @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_result_signatures        assessment_result_signatures[]
  assessment_result_statistics        assessment_result_statistics[]
  assessment_result_versions          assessment_result_versions[]
  assessment_student_results          assessment_student_results[]

  @@index([school_id], map: "idx_res_sess_school")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_signatures {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  principal_signature        String?
  coe_signature              String?
  director_signature         String?
  system_hash                String
  created_at                 DateTime                   @default(now()) @db.Timestamptz(6)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_statistics {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  pass_pct                   Decimal                    @default(100.00) @db.Decimal(5, 2)
  fail_pct                   Decimal                    @default(0.00) @db.Decimal(5, 2)
  average_gpa                Decimal                    @default(0.00) @db.Decimal(4, 2)
  median_gpa                 Decimal                    @default(0.00) @db.Decimal(4, 2)
  standard_deviation         Decimal                    @default(0.00) @db.Decimal(6, 2)
  distinction_count          Int                        @default(0)
  first_class_count          Int                        @default(0)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_result_versions {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                     @db.Uuid
  version_number             Int                        @default(1)
  calculation_hash           String
  parameters_json            Json                       @default("{}")
  created_by                 String?                    @db.Uuid
  created_at                 DateTime                   @default(now()) @db.Timestamptz(6)
  users                      public_users?              @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  assessment_result_sessions assessment_result_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_revaluation_requests {
  id               String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  attempt_id       String   @db.Uuid
  student_id       String   @db.Uuid
  fee_verified     Boolean  @default(false)
  reason           String?
  status           String   @default("REQUESTED")
  decision_remarks String?
  created_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_rubric_criteria {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  rubric_id          String             @db.Uuid
  name               String
  weight             Decimal            @default(1.00) @db.Decimal(4, 2)
  description        String?
  criteria_levels    Json               @default("[]")
  assessment_rubrics assessment_rubrics @relation(fields: [rubric_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_rubric_templates {
  id                 String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String               @db.Uuid
  name               String
  description        String?
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  schools            schools              @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_rubrics assessment_rubrics[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_rubrics {
  id                          String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                   String                       @db.Uuid
  template_id                 String?                      @db.Uuid
  question_snapshot_id        String                       @db.Uuid
  total_score                 Decimal                      @default(100.00) @db.Decimal(6, 2)
  assessment_rubric_criteria  assessment_rubric_criteria[]
  schools                     schools                      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_rubric_templates assessment_rubric_templates? @relation(fields: [template_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_semester_results {
  id             String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id     String   @db.Uuid
  semester_label String
  gpa            Decimal  @default(0.00) @db.Decimal(4, 2)
  earned_credits Int      @default(0)
  created_at     DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_sessions {
  id                          String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                   String                      @db.Uuid
  published_paper_id          String                      @db.Uuid
  academic_year_id            String                      @db.Uuid
  title                       String
  start_time                  DateTime                    @db.Timestamptz(6)
  end_time                    DateTime                    @db.Timestamptz(6)
  duration_minutes            Int
  late_join_cutoff_minutes    Int?                        @default(15)
  password                    String?
  proctoring_config           Json                        @default("{}")
  status                      String                      @default("SCHEDULED")
  created_at                  DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                  DateTime                    @default(now()) @db.Timestamptz(6)
  assessment_attempts         assessment_attempts[]
  assessment_import_logs      assessment_import_logs[]
  academic_years              academic_years              @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_published_papers assessment_published_papers @relation(fields: [published_paper_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                     schools                     @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  exam_violation_log          exam_violation_log[]

  @@index([published_paper_id], map: "idx_assessment_sessions_paper")
  @@index([start_time, end_time], map: "idx_assessment_sessions_times")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_student_results {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id                 String                       @db.Uuid
  student_id                 String                       @db.Uuid
  raw_marks_sum              Decimal                      @default(0.00) @db.Decimal(6, 2)
  scaled_marks_sum           Decimal                      @default(0.00) @db.Decimal(6, 2)
  grace_marks_sum            Decimal                      @default(0.00) @db.Decimal(6, 2)
  final_percentage           Decimal                      @default(0.00) @db.Decimal(5, 2)
  gpa                        Decimal                      @default(0.00) @db.Decimal(4, 2)
  cgpa                       Decimal                      @default(0.00) @db.Decimal(4, 2)
  total_credits              Int                          @default(0)
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  assessment_grade_cards     assessment_grade_cards[]
  assessment_result_sessions assessment_result_sessions   @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_subject_results assessment_subject_results[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model assessment_student_risk_scores {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id   String   @db.Uuid
  risk_level   String
  risk_score   Decimal  @default(0.00) @db.Decimal(4, 2)
  factors      String[]
  predicted_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_subject_results {
  id                         String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_result_id          String                     @db.Uuid
  subject_id                 String                     @db.Uuid
  awarded_marks              Decimal                    @default(0.00) @db.Decimal(6, 2)
  maximum_marks              Decimal                    @db.Decimal(6, 2)
  grade_label                String
  grade_point                Decimal                    @default(0.00) @db.Decimal(4, 2)
  assessment_student_results assessment_student_results @relation(fields: [student_result_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model assessment_subject_statistics {
  id               String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  subject_id       String   @db.Uuid
  academic_year_id String   @db.Uuid
  pass_pct         Decimal  @default(100.00) @db.Decimal(5, 2)
  average_gpa      Decimal  @default(0.00) @db.Decimal(4, 2)
  enrolled_count   Int      @default(0)
  highest_score    Decimal  @default(0.00) @db.Decimal(6, 2)
  lowest_score     Decimal  @default(0.00) @db.Decimal(6, 2)
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_sync_conflicts {
  id                  String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  attempt_id          String              @db.Uuid
  conflict_type       String
  offline_data        Json
  online_data         Json
  resolution          String              @default("PENDING")
  resolved_by         String?             @db.Uuid
  resolved_at         DateTime?           @db.Timestamptz(6)
  created_at          DateTime            @default(now()) @db.Timestamptz(6)
  assessment_attempts assessment_attempts @relation(fields: [attempt_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users               public_users?       @relation(fields: [resolved_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_footers {
  id                     String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id            String  @db.Uuid
  invigilator_signature  Boolean @default(true)
  chief_superintendent   Boolean @default(true)
  generated_timestamp    Boolean @default(true)
  page_number            Boolean @default(true)
  confidential_watermark Boolean @default(false)
  qr_verification        Boolean @default(false)
  instructions_footer    Boolean @default(true)

  @@index([template_id], map: "idx_template_footers_tmp")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_headers {
  id               String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id      String  @db.Uuid
  institution_logo Boolean @default(true)
  school_name      Boolean @default(true)
  exam_name        Boolean @default(true)
  subject          Boolean @default(true)
  class            Boolean @default(true)
  academic_year    Boolean @default(true)
  exam_date        Boolean @default(true)
  duration         Boolean @default(true)
  max_marks        Boolean @default(true)
  student_name     Boolean @default(true)
  hall_ticket      Boolean @default(true)
  signature_block  Boolean @default(true)
  qr_code          Boolean @default(false)
  barcode          Boolean @default(false)

  @@index([template_id], map: "idx_template_headers_tmp")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_instructions {
  id                String @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id       String @db.Uuid
  instructions_text String

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_layout_rules {
  id          String @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id String @db.Uuid
  property    String
  value       String

  @@index([template_id], map: "idx_template_layout_rules_tmp")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_preview_cache {
  id             String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id    String    @db.Uuid
  format         String
  hash           String
  html_path      String?
  pdf_path       String?
  thumbnail_path String?
  generated_at   DateTime  @default(now()) @db.Timestamptz(6)
  expires_at     DateTime? @db.Timestamptz(6)

  @@index([template_id], map: "idx_template_preview_cache_tmp")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_rules {
  id                           String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  section_id                   String                       @db.Uuid
  filter_field                 String
  filter_value                 String
  match_operator               String                       @default("eq")
  assessment_template_sections assessment_template_sections @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([section_id], map: "idx_assessment_t_rules_s")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_sections {
  id                        String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id               String                      @db.Uuid
  section_name              String
  description               String?
  points_per_question       Decimal                     @default(1.00) @db.Decimal(5, 2)
  negative_marks            Decimal?                    @default(0.00) @db.Decimal(5, 2)
  total_questions           Int
  sort_order                Int
  assessment_template_rules assessment_template_rules[]

  @@index([template_id], map: "idx_assessment_t_sections_t")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_template_versions {
  id                          String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  template_id                 String                        @db.Uuid
  version                     Int
  schema_snapshot             Json
  created_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  assessment_published_papers assessment_published_papers[]

  @@index([template_id], map: "idx_assessment_t_versions_t")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_transcripts {
  id                   String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id           String        @db.Uuid
  academic_record_json Json          @default("{}")
  is_official          Boolean       @default(false)
  issued_by            String?       @db.Uuid
  issued_at            DateTime      @default(now()) @db.Timestamptz(6)
  users                public_users? @relation(fields: [issued_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_workflow_definitions {
  id                              String                            @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                       String                            @db.Uuid
  name                            String
  description                     String?
  is_active                       Boolean                           @default(true)
  version                         Int                               @default(1)
  is_deleted                      Boolean                           @default(false)
  deleted_at                      DateTime?                         @db.Timestamptz(6)
  deleted_by                      String?                           @db.Uuid
  created_at                      DateTime                          @default(now()) @db.Timestamptz(6)
  updated_at                      DateTime                          @default(now()) @db.Timestamptz(6)
  users                           public_users?                     @relation(fields: [deleted_by], references: [id], onUpdate: NoAction)
  schools                         schools                           @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_workflow_steps       assessment_workflow_steps[]
  assessment_workflow_transitions assessment_workflow_transitions[]

  @@unique([school_id, name], map: "unique_school_workflow_name")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_workflow_steps {
  id                              String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workflow_id                     String                          @db.Uuid
  step_name                       String
  role_required                   String
  sort_order                      Int
  created_at                      DateTime                        @default(now()) @db.Timestamptz(6)
  assessment_workflow_definitions assessment_workflow_definitions @relation(fields: [workflow_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([workflow_id, sort_order], map: "unique_workflow_sort_order")
  @@index([workflow_id], map: "idx_assessment_workflow_step_wf")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assessment_workflow_transitions {
  id                              String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workflow_id                     String                          @db.Uuid
  from_status                     String
  to_status                       String
  rule_condition                  String?
  created_at                      DateTime                        @default(now()) @db.Timestamptz(6)
  assessment_workflow_definitions assessment_workflow_definitions @relation(fields: [workflow_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([workflow_id, from_status, to_status], map: "unique_workflow_transition")
  @@index([workflow_id], map: "idx_assessment_workflow_trans_wf")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model assignment_submissions {
  id               String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  assignment_id    String        @db.Uuid
  student_id       String        @db.Uuid
  submission_text  String?
  submission_files String[]
  submitted_at     DateTime?     @default(now()) @db.Timestamptz(6)
  marks_obtained   Decimal?      @db.Decimal(5, 2)
  feedback         String?
  graded_at        DateTime?     @db.Timestamptz(6)
  graded_by        String?       @db.Uuid
  users            public_users? @relation(fields: [graded_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  students         students      @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([assignment_id, student_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model attendance_calendar_days {
  id                   String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  calendar_id          String               @db.Uuid
  day_date             DateTime             @db.Date
  day_type             String               @default("WORKING")
  remarks              String?
  attendance_calendars attendance_calendars @relation(fields: [calendar_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model attendance_calendars {
  id                       String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                String                     @db.Uuid
  campus_id                String                     @db.Uuid
  branch_id                String                     @db.Uuid
  academic_year_id         String                     @db.Uuid
  calendar_name            String
  created_at               DateTime                   @default(now()) @db.Timestamptz(6)
  attendance_calendar_days attendance_calendar_days[]
  schools                  schools                    @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model attendance_device_logs {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  device_id          String             @db.Uuid
  student_id         String             @db.Uuid
  scan_timestamp     DateTime           @db.Timestamptz(6)
  attendance_devices attendance_devices @relation(fields: [device_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model attendance_device_queue {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  device_id          String             @db.Uuid
  raw_payload        Json               @default("{}")
  status             String             @default("PENDING")
  retry_count        Int                @default(0)
  attendance_devices attendance_devices @relation(fields: [device_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model attendance_devices {
  id                      String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id               String                    @db.Uuid
  device_name             String
  device_ip               String
  status                  String                    @default("ONLINE")
  attendance_device_logs  attendance_device_logs[]
  attendance_device_queue attendance_device_queue[]
  schools                 schools                   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model attendance_eligibility {
  id                    String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id            String   @db.Uuid
  subject_id            String   @db.Uuid
  attendance_percentage Decimal  @default(0.00) @db.Decimal(5, 2)
  is_eligible           Boolean  @default(true)
  updated_at            DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model attendance_event_outbox {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  event_name String
  payload    Json     @default("{}")
  status     String   @default("PENDING")
  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model attendance_exception_requests {
  id             String @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id     String @db.Uuid
  session_id     String @db.Uuid
  exception_type String
  reason         String
  status         String @default("PENDING")

  @@schema("public")
}

model attendance_holidays {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id    String   @db.Uuid
  holiday_date DateTime @db.Date
  holiday_name String
  schools      schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model attendance_logs {
  id              String          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id       String          @db.Uuid
  attendance_id   String          @db.Uuid
  action          String
  details         Json            @default("{}")
  performed_by    String?         @db.Uuid
  created_at      DateTime        @default(now()) @db.Timestamptz(6)
  exam_attendance exam_attendance @relation(fields: [attendance_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users           public_users?   @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  schools         schools         @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model attendance_makeup_classes {
  id                      String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id               String   @db.Uuid
  makeup_date             DateTime @db.Date
  original_timetable_date DateTime @db.Date
  schools                 schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model attendance_policies {
  id                     String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id              String  @db.Uuid
  minimum_percentage     Decimal @default(75.00) @db.Decimal(5, 2)
  late_threshold_minutes Int     @default(15)
  condonation_limit      Int     @default(5)
  schools                schools @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model attendance_record_versions {
  id                   String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  attendance_record_id String        @db.Uuid
  previous_snapshot    Json          @default("{}")
  changed_by           String?       @db.Uuid
  changed_reason       String
  changed_at           DateTime      @default(now()) @db.Timestamptz(6)
  users                public_users? @relation(fields: [changed_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

model attendance_session_locks {
  id         String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id String        @db.Uuid
  locked_by  String?       @db.Uuid
  locked_at  DateTime      @default(now()) @db.Timestamptz(6)
  reason     String
  users      public_users? @relation(fields: [locked_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model attendance_session_workflow {
  id          String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  session_id  String        @db.Uuid
  approved_by String?       @db.Uuid
  role_level  String
  decision    String
  comments    String?
  users       public_users? @relation(fields: [approved_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

model attendance_special_days {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id    String   @db.Uuid
  special_date DateTime @db.Date
  description  String
  schools      schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_logs {
  id             String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id        String?       @db.Uuid
  action         String
  entity_name    String
  entity_id      String        @db.Uuid
  before_state   Json?
  after_state    Json?
  ip_address     String?
  user_agent     String?
  correlation_id String?       @db.Uuid
  created_at     DateTime?     @default(now()) @db.Timestamptz(6)
  users          public_users? @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@index([correlation_id], map: "idx_audit_logs_correlation")
  @@index([entity_name, entity_id], map: "idx_audit_logs_entity")
  @@schema("public")
}

model business_rules {
  id             String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id      String?   @db.Uuid
  name           String
  event_trigger  String
  rules_config   Json      @default("{}")
  actions_config Json      @default("[]")
  is_active      Boolean   @default(true)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  schools        schools?  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model classes {
  id                            String                          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                     String                          @db.Uuid
  academic_year_id              String                          @db.Uuid
  name                          String
  created_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  academic_years                academic_years                  @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                       schools                         @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  finance_fee_structure_classes finance_fee_structure_classes[]
  sections                      sections[]
  subjects                      subjects[]

  @@unique([school_id, academic_year_id, name])
  @@index([school_id], map: "idx_classes_school_id")
  @@schema("public")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model default_school_id {
  id String? @db.Uuid

  @@ignore
  @@schema("public")
}

model degree_requirement_courses {
  id                        String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  group_id                  String                    @db.Uuid
  subject_id                String                    @db.Uuid
  is_core                   Boolean                   @default(true)
  degree_requirement_groups degree_requirement_groups @relation(fields: [group_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model degree_requirement_groups {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  program_id                 String                       @db.Uuid
  group_name                 String
  required_credits           Int                          @default(0)
  degree_requirement_courses degree_requirement_courses[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model departments {
  id               String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String             @db.Uuid
  name             String
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  schools          schools            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  faculty_profiles faculty_profiles[]
  staff_profiles   staff_profiles[]

  @@unique([school_id, name])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_checklists {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String         @db.Uuid
  academic_year_id String         @db.Uuid
  grade            String
  admission_type   String         @default("Regular")
  document_type_id String         @db.Uuid
  mandatory        Boolean        @default(true)
  minimum_copies   Int            @default(1)
  created_at       DateTime?      @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  document_types   document_types @relation(fields: [document_type_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools          schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, academic_year_id, grade, admission_type, document_type_id], map: "unique_checklist_rule")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_comments {
  id                    String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  document_id           String                @db.Uuid
  comment               String
  comment_type          String                @default("REVIEWER")
  created_by            String?               @db.Uuid
  created_at            DateTime?             @default(now()) @db.Timestamptz(6)
  users                 public_users?         @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  application_documents application_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_types {
  id                    String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  code                  String                  @unique
  name                  String
  description           String?
  category              String
  mandatory             Boolean                 @default(false)
  allowed_extensions    String[]                @default(["pdf", "jpg", "jpeg", "png"])
  allowed_mime_types    String[]                @default(dbgenerated("'{application/pdf,image/jpeg,image/png}'::text[]"))
  max_file_size         Int                     @default(5242880)
  active                Boolean                 @default(true)
  display_order         Int?                    @default(0)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?               @default(now()) @db.Timestamptz(6)
  application_documents application_documents[]
  document_checklists   document_checklists[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_verification {
  id                    String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  document_id           String                @db.Uuid
  status                String
  reviewer_id           String?               @db.Uuid
  remarks               String?
  verified_at           DateTime?             @default(now()) @db.Timestamptz(6)
  created_at            DateTime?             @default(now()) @db.Timestamptz(6)
  application_documents application_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 public_users?         @relation(fields: [reviewer_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_versions {
  id                    String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  document_id           String                @db.Uuid
  version               Int
  storage_path          String
  checksum              String
  uploaded_by           String?               @db.Uuid
  uploaded_at           DateTime?             @default(now()) @db.Timestamptz(6)
  application_documents application_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 public_users?         @relation(fields: [uploaded_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model document_workflow_rules {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_status String
  to_status   String
  role        String
  allowed     Boolean? @default(true)

  @@unique([from_status, to_status, role], map: "unique_doc_workflow_rule")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model enrollment_workflow_rules {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_status String
  to_status   String
  role        String
  allowed     Boolean? @default(true)

  @@unique([from_status, to_status, role], map: "unique_enrollment_workflow")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_attendance {
  id               String            @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  exam_schedule_id String            @db.Uuid
  student_id       String            @db.Uuid
  hall_id          String?           @db.Uuid
  status           String?           @default("PRESENT")
  remarks          String?
  marked_by        String?           @db.Uuid
  marked_at        DateTime?         @default(now()) @db.Timestamptz(6)
  school_id        String?           @db.Uuid
  entry_time       DateTime?         @db.Timestamptz(6)
  verified_via     String?
  updated_at       DateTime?         @default(now()) @db.Timestamptz(6)
  attendance_logs  attendance_logs[]
  exam_halls       exam_halls?       @relation(fields: [hall_id], references: [id], onUpdate: NoAction)
  users            public_users?     @relation(fields: [marked_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  schools          schools?          @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students          @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([exam_schedule_id, student_id])
  @@index([exam_schedule_id], map: "idx_exam_att_schedule")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_audit_logs {
  id           String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  entity_type  String
  entity_id    String?   @db.Uuid
  action       String
  performed_by String?   @db.Uuid
  reason       String?
  created_at   DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_buildings {
  id           String       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id    String       @db.Uuid
  center_id    String       @db.Uuid
  name         String
  floors_count Int          @default(1)
  is_active    Boolean      @default(true)
  created_at   DateTime     @default(now()) @db.Timestamptz(6)
  updated_at   DateTime     @default(now()) @db.Timestamptz(6)
  exam_centers exam_centers @relation(fields: [center_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools      schools      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  exam_rooms   exam_rooms[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_centers {
  id             String           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id      String           @db.Uuid
  name           String
  campus         String?
  code           String
  is_active      Boolean          @default(true)
  created_at     DateTime         @default(now()) @db.Timestamptz(6)
  updated_at     DateTime         @default(now()) @db.Timestamptz(6)
  exam_buildings exam_buildings[]
  schools        schools          @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, code], map: "unique_school_center_code")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_eligibility_snapshots {
  id                    String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  exam_id               String        @db.Uuid
  student_id            String        @db.Uuid
  eligible              Boolean
  attendance_percentage Decimal?      @db.Decimal(5, 2)
  fees_status           String?
  reasons               Json?         @default("[]")
  captured_at           DateTime?     @default(now()) @db.Timestamptz(6)
  is_overridden         Boolean?      @default(false)
  override_reason       String?
  overridden_by         String?       @db.Uuid
  overridden_at         DateTime?     @db.Timestamptz(6)
  original_eligible     Boolean?
  source                String?       @default("REAL")
  exams                 exams         @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 public_users? @relation(fields: [overridden_by], references: [id], onUpdate: NoAction)
  students              students      @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([exam_id, student_id])
  @@index([exam_id], map: "idx_elig_snap_exam")
  @@index([student_id], map: "idx_elig_snap_student")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_halls {
  id                       String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                String                     @db.Uuid
  hall_name                String
  capacity                 Int
  location                 String?
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  rows_count               Int?                       @default(5)
  cols_count               Int?                       @default(5)
  exam_attendance          exam_attendance[]
  schools                  schools                    @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  exam_seating_allocations exam_seating_allocations[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_question_papers {
  id               String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  exam_schedule_id String    @db.Uuid
  uploaded_by      String    @db.Uuid
  file_url         String
  file_name        String
  version          Int?      @default(1)
  status           String?   @default("DRAFT")
  uploaded_at      DateTime? @default(now()) @db.Timestamptz(6)
  locked_at        DateTime? @db.Timestamptz(6)

  @@unique([exam_schedule_id, version], map: "unique_schedule_version")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_registrations {
  id                          String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                   String                        @db.Uuid
  student_id                  String                        @db.Uuid
  exam_id                     String                        @db.Uuid
  status                      String?                       @default("DRAFT")
  created_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  updated_at                  DateTime                      @default(now()) @db.Timestamptz(6)
  exams                       exams                         @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                     schools                       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students                    students                      @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  hall_tickets                hall_tickets[]
  registration_documents      registration_documents[]
  registration_logs           registration_logs[]
  registration_status_history registration_status_history[]

  @@unique([student_id, exam_id], map: "unique_student_exam_registration")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_result_publications {
  id           String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id    String    @db.Uuid
  exam_id      String    @db.Uuid
  status       String?   @default("EVALUATED")
  published_at DateTime? @db.Timestamptz(6)
  frozen       Boolean   @default(false)
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  updated_at   DateTime  @default(now()) @db.Timestamptz(6)
  exams        exams     @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools      schools   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, exam_id], map: "unique_result_exam_publication")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_rooms {
  id                      String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id               String                    @db.Uuid
  building_id             String                    @db.Uuid
  room_number             String
  floor_number            Int                       @default(0)
  capacity                Int
  accessibility_supported Boolean                   @default(false)
  rows_count              Int                       @default(5)
  cols_count              Int                       @default(5)
  is_active               Boolean                   @default(true)
  created_at              DateTime                  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime                  @default(now()) @db.Timestamptz(6)
  exam_buildings          exam_buildings            @relation(fields: [building_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                 schools                   @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  invigilator_assignments invigilator_assignments[]
  schedule_rooms          schedule_rooms[]
  seat_allocations        seat_allocations[]
  seat_layouts            seat_layouts[]

  @@unique([building_id, room_number], map: "unique_building_room")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_seating_allocations {
  id               String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  exam_schedule_id String     @db.Uuid
  student_id       String     @db.Uuid
  hall_id          String     @db.Uuid
  seat_number      String
  created_at       DateTime?  @default(now()) @db.Timestamptz(6)
  exam_id          String?    @db.Uuid
  exams            exams?     @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  exam_halls       exam_halls @relation(fields: [hall_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students   @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([exam_schedule_id, hall_id, seat_number], map: "unique_hall_seat_schedule")
  @@unique([exam_id, student_id], map: "unique_student_exam_seat")
  @@unique([exam_schedule_id, student_id], map: "unique_student_schedule_seat")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_subjects {
  exam_id    String   @db.Uuid
  subject_id String   @db.Uuid
  max_marks  Decimal? @default(100.00) @db.Decimal(5, 2)
  exams      exams    @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects   subjects @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([exam_id, subject_id])
  @@index([exam_id], map: "idx_exam_subjects_exam_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_violation_log {
  id                  String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  attempt_id          String              @db.Uuid
  student_id          String              @db.Uuid
  session_id          String              @db.Uuid
  violation_type      String
  timestamp           DateTime            @default(now()) @db.Timestamptz(6)
  browser_metadata    Json                @default("{}")
  user_agent          String?
  ip_address          String?
  severity            String              @default("MEDIUM")
  created_at          DateTime            @default(now()) @db.Timestamptz(6)
  assessment_attempts assessment_attempts @relation(fields: [attempt_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  assessment_sessions assessment_sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students            students            @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([attempt_id], map: "idx_exam_violation_log_attempt")
  @@index([session_id], map: "idx_exam_violation_log_session")
  @@index([student_id], map: "idx_exam_violation_log_student")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exam_workflow_rules {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_status String
  to_status   String
  role        String
  allowed     Boolean? @default(true)

  @@unique([from_status, to_status, role], map: "unique_exam_workflow")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exams {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                  String                       @db.Uuid
  academic_year_id           String                       @db.Uuid
  name                       String
  start_date                 DateTime                     @db.Date
  end_date                   DateTime                     @db.Date
  created_at                 DateTime?                    @default(now()) @db.Timestamptz(6)
  created_by                 String?                      @db.Uuid
  type                       String?
  applicable_classes         String[]                     @db.Uuid
  term                       String?                      @default("ANNUAL")
  eligibility_frozen         Boolean?                     @default(false)
  eligibility_frozen_at      DateTime?                    @db.Timestamptz(6)
  status                     String?                      @default("DRAFT")
  seating_status             String?                      @default("DRAFT")
  exam_eligibility_snapshots exam_eligibility_snapshots[]
  exam_registrations         exam_registrations[]
  exam_result_publications   exam_result_publications[]
  exam_seating_allocations   exam_seating_allocations[]
  exam_subjects              exam_subjects[]
  academic_years             academic_years               @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                      public_users?                @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  schools                    schools                      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  marks                      marks[]
  student_result_summaries   student_result_summaries[]

  @@unique([school_id, academic_year_id, name])
  @@index([school_id], map: "idx_exams_school_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model faculty_profiles {
  id                       String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id                  String?                    @unique @db.Uuid
  department_id            String?                    @db.Uuid
  employee_code            String?                    @unique
  designation              String?
  qualification            String?
  joining_date             DateTime?                  @db.Date
  status                   String?                    @default("active")
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  departments              departments?               @relation(fields: [department_id], references: [id], onUpdate: NoAction)
  users                    public_users?              @relation(fields: [user_id], references: [id], onUpdate: NoAction)
  faculty_section_subjects faculty_section_subjects[]
  invigilator_assignments  invigilator_assignments[]
  invigilator_availability invigilator_availability[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model faculty_section_subjects {
  id                 String           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  faculty_profile_id String           @db.Uuid
  section_id         String           @db.Uuid
  subject_id         String           @db.Uuid
  assigned_by        String?          @db.Uuid
  created_at         DateTime?        @default(now()) @db.Timestamptz(6)
  users              public_users?    @relation(fields: [assigned_by], references: [id], onUpdate: NoAction)
  faculty_profiles   faculty_profiles @relation(fields: [faculty_profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sections           sections         @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects           subjects         @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([faculty_profile_id, section_id, subject_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model faculty_sections {
  faculty_user_id String       @db.Uuid
  section_id      String       @db.Uuid
  role            String?      @default("subject_teacher")
  assigned_at     DateTime?    @default(now()) @db.Timestamptz(6)
  users           public_users @relation(fields: [faculty_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sections        sections     @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([faculty_user_id, section_id])
  @@index([faculty_user_id], map: "idx_faculty_sections_faculty_id")
  @@index([section_id], map: "idx_faculty_sections_section_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feature_flags {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  module      String
  feature_key String
  enabled     Boolean   @default(false)
  environment String    @default("development")
  tenant_id   String?   @db.Uuid
  description String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)
  schools     schools?  @relation(fields: [tenant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([module, feature_key, environment, tenant_id], map: "unique_feature_flag")
  @@unique([module, feature_key, environment, tenant_id], map: "uq_feature_flags")
  @@index([module, feature_key, environment], map: "idx_feature_flags_lookup")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model fee_demand_items {
  id        String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  demand_id String  @db.Uuid
  name      String
  category  String
  amount    Decimal @db.Decimal(10, 2)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model fee_receipts {
  id                                     String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  receipt_no                             String               @unique
  payment_transaction_id                 String               @db.Uuid
  receipt_type                           String               @default("ORIGINAL")
  pdf_url                                String?
  status                                 String               @default("GENERATED")
  generated_by                           String?              @db.Uuid
  generated_at                           DateTime?            @default(now()) @db.Timestamptz(6)
  print_count                            Int                  @default(0)
  emailed                                Boolean              @default(false)
  sms_sent                               Boolean              @default(false)
  created_at                             DateTime?            @default(now()) @db.Timestamptz(6)
  collected_by                           String?              @db.Uuid
  email_sent                             Boolean              @default(false)
  emailed_at                             DateTime?            @db.Timestamptz(6)
  users_fee_receipts_collected_byTousers auth_users?          @relation("fee_receipts_collected_byTousers", fields: [collected_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users_fee_receipts_generated_byTousers auth_users?          @relation("fee_receipts_generated_byTousers", fields: [generated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  payment_transactions                   payment_transactions @relation(fields: [payment_transaction_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([receipt_no], map: "idx_fee_receipts_receipt_no")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model fee_reminders {
  id                     String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String?                 @db.Uuid
  student_id             String?                 @db.Uuid
  channel                String
  status                 String                  @default("PENDING")
  provider               String?
  attempt_count          Int                     @default(0)
  last_attempt           DateTime?               @db.Timestamptz(6)
  response               String?
  created_at             DateTime?               @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications? @relation(fields: [application_id], references: [id], onUpdate: NoAction)
  students               students?               @relation(fields: [student_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model finance_audit_logs {
  id           String      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  action       String
  entity_type  String
  entity_id    String      @db.Uuid
  performed_by String?     @db.Uuid
  details      Json?
  created_at   DateTime?   @default(now()) @db.Timestamptz(6)
  school_id    String?     @db.Uuid
  user_name    String?
  reference_no String?
  users        auth_users? @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  schools      schools?    @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at], map: "idx_finance_audit_logs_created")
  @@index([school_id], map: "idx_finance_audit_logs_school")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model finance_fee_installments {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  fee_structure_id       String                 @db.Uuid
  term                   String
  due_date               DateTime               @db.Date
  percentage             Decimal?               @db.Decimal(5, 2)
  fixed_amount           Decimal?               @db.Decimal(10, 2)
  finance_fee_structures finance_fee_structures @relation(fields: [fee_structure_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model finance_fee_structure_classes {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  fee_structure_id       String                 @db.Uuid
  class_id               String                 @db.Uuid
  classes                classes                @relation(fields: [class_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  finance_fee_structures finance_fee_structures @relation(fields: [fee_structure_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([fee_structure_id, class_id])
  @@index([fee_structure_id], map: "idx_finance_fee_structure_classes_struct")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model finance_fee_structure_components {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  fee_structure_id       String                 @db.Uuid
  name                   String
  category               String
  amount                 Decimal                @db.Decimal(10, 2)
  display_order          Int                    @default(0)
  is_mandatory           Boolean                @default(true)
  finance_fee_structures finance_fee_structures @relation(fields: [fee_structure_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model finance_fee_structures {
  id                               String                             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                        String                             @db.Uuid
  academic_year_id                 String                             @db.Uuid
  name                             String
  version                          Int                                @default(1)
  effective_from                   DateTime                           @db.Date
  effective_to                     DateTime                           @db.Date
  is_active                        Boolean                            @default(true)
  published_by                     String?                            @db.Uuid
  published_at                     DateTime?                          @db.Timestamptz(6)
  created_at                       DateTime?                          @default(now()) @db.Timestamptz(6)
  finance_fee_installments         finance_fee_installments[]
  finance_fee_structure_classes    finance_fee_structure_classes[]
  finance_fee_structure_components finance_fee_structure_components[]
  academic_years                   academic_years                     @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                            auth_users?                        @relation(fields: [published_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  schools                          schools                            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, academic_year_id, name, version], map: "unique_school_year_name_version")
  @@index([school_id], map: "idx_finance_fee_structures_school")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model finance_settings {
  id                          String      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                   String      @unique @db.Uuid
  receipt_prefix              String      @default("RCPT")
  demand_prefix               String      @default("DEM")
  currency                    String      @default("INR")
  currency_symbol             String      @default("₹")
  late_fee_enabled            Boolean     @default(false)
  late_fee_percentage         Decimal     @default(0.00) @db.Decimal(5, 2)
  grace_days                  Int         @default(0)
  default_payment_window_days Int         @default(30)
  receipt_footer              String?
  school_year_label           String?
  updated_by                  String?     @db.Uuid
  updated_at                  DateTime?   @default(now()) @db.Timestamptz(6)
  schools                     schools     @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                       auth_users? @relation(fields: [updated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model grading_scales {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id   String    @db.Uuid
  min_score   Decimal   @db.Decimal
  max_score   Decimal   @db.Decimal
  grade_label String
  grade_point Decimal?  @default(0) @db.Decimal
  created_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model graduation_audit {
  id                String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id        String   @db.Uuid
  audit_status      String   @default("INCOMPLETE")
  credits_completed Int      @default(0)
  cgpa_score        Decimal  @default(0.00) @db.Decimal(4, 2)
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model graduation_candidates {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String   @db.Uuid
  status     String   @default("ELIGIBLE")
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model graduation_clearance_items {
  id             String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id     String   @db.Uuid
  clearance_type String
  status         String   @default("PENDING")
  updated_at     DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model graduation_requirements {
  id          String  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id   String  @db.Uuid
  min_credits Int     @default(120)
  min_cgpa    Decimal @default(6.00) @db.Decimal(4, 2)
  schools     schools @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model hall_ticket_logs {
  id             String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id      String        @db.Uuid
  hall_ticket_id String        @db.Uuid
  action         String
  remarks        String?
  performed_by   String?       @db.Uuid
  created_at     DateTime      @default(now()) @db.Timestamptz(6)
  hall_tickets   hall_tickets  @relation(fields: [hall_ticket_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users          public_users? @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  schools        schools       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model hall_tickets {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String             @db.Uuid
  registration_id    String             @db.Uuid
  ticket_code        String             @unique
  status             String?            @default("GENERATED")
  snapshot_data      Json               @default("{}")
  generated_at       DateTime           @default(now()) @db.Timestamptz(6)
  updated_at         DateTime           @default(now()) @db.Timestamptz(6)
  hall_ticket_logs   hall_ticket_logs[]
  exam_registrations exam_registrations @relation(fields: [registration_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model import_jobs {
  id            String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id     String        @db.Uuid
  entity_type   String
  file_name     String?
  status        String?       @default("PENDING")
  total_rows    Int?          @default(0)
  success_count Int?          @default(0)
  failed_count  Int?          @default(0)
  failed_rows   Json?         @default("[]")
  created_by    String?       @db.Uuid
  created_at    DateTime?     @default(now()) @db.Timestamptz(6)
  updated_at    DateTime?     @default(now()) @db.Timestamptz(6)
  users         public_users? @relation(fields: [created_by], references: [id], onUpdate: NoAction)
  schools       schools       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([school_id], map: "idx_import_jobs_school")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model interview_workflow_rules {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_status String
  to_status   String
  role        String
  allowed     Boolean? @default(true)

  @@unique([from_status, to_status, role], map: "unique_interview_workflow")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model invigilator_assignments {
  id                 String           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String           @db.Uuid
  exam_schedule_id   String           @db.Uuid
  room_id            String           @db.Uuid
  faculty_profile_id String           @db.Uuid
  role               String?          @default("INVIGILATOR")
  status             String?          @default("ASSIGNED")
  created_at         DateTime         @default(now()) @db.Timestamptz(6)
  faculty_profiles   faculty_profiles @relation(fields: [faculty_profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  exam_rooms         exam_rooms       @relation(fields: [room_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools          @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([exam_schedule_id, faculty_profile_id], map: "unique_invigilator_schedule")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model invigilator_availability {
  id                 String           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String           @db.Uuid
  faculty_profile_id String           @db.Uuid
  available_date     DateTime         @db.Date
  start_time         DateTime         @db.Time(6)
  end_time           DateTime         @db.Time(6)
  is_available       Boolean          @default(true)
  created_at         DateTime         @default(now()) @db.Timestamptz(6)
  faculty_profiles   faculty_profiles @relation(fields: [faculty_profile_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools          @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([faculty_profile_id, available_date, start_time])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model marks {
  student_id     String        @db.Uuid
  exam_id        String        @db.Uuid
  subject_id     String        @db.Uuid
  marks_obtained Decimal       @db.Decimal(5, 2)
  entered_by     String?       @db.Uuid
  entered_at     DateTime?     @default(now()) @db.Timestamptz(6)
  status         String?       @default("PRESENT")
  is_locked      Boolean?      @default(false)
  remarks        String?
  users          public_users? @relation(fields: [entered_by], references: [id], onUpdate: NoAction)
  exams          exams         @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students       students      @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects       subjects      @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([student_id, exam_id, subject_id])
  @@index([exam_id], map: "idx_marks_exam_id")
  @@index([student_id], map: "idx_marks_student_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notification_delivery_logs {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String             @db.Uuid
  queue_id           String             @db.Uuid
  status             String
  error_message      String?
  attempt_number     Int
  created_at         DateTime           @default(now()) @db.Timestamptz(6)
  notification_queue notification_queue @relation(fields: [queue_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notification_queue {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                  String                       @db.Uuid
  user_id                    String                       @db.Uuid
  template_code              String?
  channel                    String?                      @default("IN_APP")
  title                      String
  body                       String
  status                     String?                      @default("PENDING")
  retry_count                Int                          @default(0)
  scheduled_at               DateTime                     @default(now()) @db.Timestamptz(6)
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  notification_delivery_logs notification_delivery_logs[]
  schools                    schools                      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  notification_templates     notification_templates?      @relation(fields: [template_code], references: [code], onUpdate: NoAction)
  users                      public_users                 @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notification_templates {
  code               String               @id
  school_id          String               @db.Uuid
  channel            String
  subject            String?
  body_template      String
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  notification_queue notification_queue[]
  schools            schools              @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notifications {
  id         String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id    String     @db.Uuid
  title      String
  body       String
  channel    String     @default("IN_APP")
  is_read    Boolean?   @default(false)
  metadata   Json?      @default("{}")
  created_at DateTime?  @default(now()) @db.Timestamptz(6)
  users      auth_users @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model offer_workflow_rules {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_status String
  to_status   String
  role        String
  allowed     Boolean? @default(true)

  @@unique([from_status, to_status, role], map: "unique_offer_workflow")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model official_transcripts {
  id                    String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id            String                  @db.Uuid
  pdf_url               String
  is_official           Boolean                 @default(true)
  created_at            DateTime                @default(now()) @db.Timestamptz(6)
  transcript_signatures transcript_signatures[]
  transcript_versions   transcript_versions[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payment_allocations {
  id               String       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  payment_id       String       @db.Uuid
  student_fee_id   String       @db.Uuid
  amount_allocated Decimal      @db.Decimal(10, 2)
  created_at       DateTime?    @default(now()) @db.Timestamptz(6)
  payments         payments     @relation(fields: [payment_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  student_fees     student_fees @relation(fields: [student_fee_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([payment_id, student_fee_id])
  @@index([student_fee_id], map: "idx_payment_allocations_fee")
  @@index([payment_id], map: "idx_payment_allocations_payment")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payment_transactions {
  id                                            String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id                                String?                 @db.Uuid
  student_id                                    String?                 @db.Uuid
  amount                                        Decimal                 @db.Decimal(10, 2)
  payment_mode                                  String
  transaction_reference                         String?
  bank_name                                     String?
  gateway_name                                  String?
  gateway_transaction_id                        String?
  gateway_status                                String?
  gateway_error_code                            String?
  gateway_error_message                         String?
  gateway_response                              Json?
  payment_channel                               String?
  cashier_id                                    String?                 @db.Uuid
  verified_by                                   String?                 @db.Uuid
  status                                        String                  @default("PENDING")
  created_at                                    DateTime?               @default(now()) @db.Timestamptz(6)
  fee_receipts                                  fee_receipts[]
  admission_applications                        admission_applications? @relation(fields: [application_id], references: [id], onUpdate: NoAction)
  users_payment_transactions_cashier_idTousers  auth_users?             @relation("payment_transactions_cashier_idTousers", fields: [cashier_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  students                                      students?               @relation(fields: [student_id], references: [id], onUpdate: NoAction)
  users_payment_transactions_verified_byTousers auth_users?             @relation("payment_transactions_verified_byTousers", fields: [verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([application_id], map: "idx_payment_transactions_application")
  @@index([created_at], map: "idx_payment_transactions_created")
  @@index([payment_mode], map: "idx_payment_transactions_mode")
  @@index([status], map: "idx_payment_transactions_status")
  @@index([student_id], map: "idx_payment_transactions_student")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payments {
  id                  String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id          String                @db.Uuid
  amount_paid         Decimal               @db.Decimal(10, 2)
  payment_date        DateTime              @default(dbgenerated("CURRENT_DATE")) @db.Date
  payment_mode        String                @default("cash")
  reference_no        String?
  remarks             String?
  created_at          DateTime?             @default(now()) @db.Timestamptz(6)
  payment_allocations payment_allocations[]
  students            students              @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([student_id], map: "idx_payments_student_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model permissions {
  id               String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  code             String             @unique
  description      String?
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  role_permissions role_permissions[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model registration_documents {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String             @db.Uuid
  registration_id    String             @db.Uuid
  document_name      String
  document_url       String
  uploaded_at        DateTime           @default(now()) @db.Timestamptz(6)
  exam_registrations exam_registrations @relation(fields: [registration_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model registration_logs {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String             @db.Uuid
  registration_id    String             @db.Uuid
  action             String
  details            Json               @default("{}")
  performed_by       String?            @db.Uuid
  created_at         DateTime           @default(now()) @db.Timestamptz(6)
  users              public_users?      @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  exam_registrations exam_registrations @relation(fields: [registration_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model registration_status_history {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String             @db.Uuid
  registration_id    String             @db.Uuid
  status             String
  changed_by         String?            @db.Uuid
  remarks            String?
  created_at         DateTime           @default(now()) @db.Timestamptz(6)
  users              public_users?      @relation(fields: [changed_by], references: [id], onUpdate: NoAction)
  exam_registrations exam_registrations @relation(fields: [registration_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model request_tracking {
  id              String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  request_id      String?       @db.Uuid
  idempotency_key String?       @unique
  module          String
  operation       String
  user_id         String?       @db.Uuid
  request_hash    String?
  status          String
  response_code   Int
  response_body   Json
  expires_at      DateTime?     @db.Timestamptz(6)
  created_at      DateTime?     @default(now()) @db.Timestamptz(6)
  users           public_users? @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@index([idempotency_key], map: "idx_request_tracking_key")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model result_audit_logs {
  id               String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String        @db.Uuid
  target_result_id String        @db.Uuid
  action_type      String
  old_value        Json?
  new_value        Json?
  performed_by     String?       @db.Uuid
  created_at       DateTime      @default(now()) @db.Timestamptz(6)
  users            public_users? @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  schools          schools       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model result_publications {
  id               String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String             @db.Uuid
  academic_year_id String             @db.Uuid
  exam_term        String
  published_at     DateTime?          @db.Timestamptz(6)
  published_by     String?            @db.Uuid
  is_released      Boolean            @default(false)
  created_at       DateTime           @default(now()) @db.Timestamptz(6)
  approval_history approval_history[]
  academic_years   academic_years     @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users            public_users?      @relation(fields: [published_by], references: [id], onUpdate: NoAction)
  schools          schools            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, academic_year_id, exam_term], map: "unique_school_year_term_publication")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model result_rules {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String         @db.Uuid
  academic_year_id String         @db.Uuid
  rule_name        String
  rule_type        String
  rule_config      Json           @default("{}")
  created_at       DateTime       @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools          schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, academic_year_id, rule_name], map: "unique_school_year_rule")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model role_permissions {
  role_id       String      @db.Uuid
  permission_id String      @db.Uuid
  permissions   permissions @relation(fields: [permission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  roles         roles       @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([role_id, permission_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model roles {
  id               String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name             String             @unique
  description      String?
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  role_permissions role_permissions[]
  user_roles       user_roles[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model route_vehicles {
  route_id          String             @db.Uuid
  vehicle_id        String             @db.Uuid
  driver_id         String?            @db.Uuid
  transport_drivers transport_drivers? @relation(fields: [driver_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@id([route_id, vehicle_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schedule_rooms {
  id                 String     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id          String     @db.Uuid
  exam_schedule_id   String     @db.Uuid
  room_id            String     @db.Uuid
  allocated_capacity Int
  created_at         DateTime   @default(now()) @db.Timestamptz(6)
  updated_at         DateTime   @default(now()) @db.Timestamptz(6)
  exam_rooms         exam_rooms @relation(fields: [room_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools            schools    @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([exam_schedule_id, room_id], map: "unique_schedule_room")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schedule_sessions {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id  String   @db.Uuid
  name       String
  start_time DateTime @db.Time(6)
  end_time   DateTime @db.Time(6)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)
  schools    schools  @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schools {
  id                                String                              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name                              String
  code                              String                              @unique
  status                            String?                             @default("active")
  created_at                        DateTime?                           @default(now()) @db.Timestamptz(6)
  academic_automation_logs          academic_automation_logs[]
  academic_standing_rules           academic_standing_rules[]
  academic_years                    academic_years[]
  admission_applications            admission_applications[]
  admission_assessment_attempts     admission_assessment_attempts[]
  admission_assessment_outbox       admission_assessment_outbox[]
  admission_assessment_policies     admission_assessment_policies[]
  admission_assessment_sessions     admission_assessment_sessions[]
  admission_assessment_snapshots    admission_assessment_snapshots[]
  admission_conversion_metrics      admission_conversion_metrics[]
  admission_enquiries               admission_enquiries[]
  admission_exam_schedule           admission_exam_schedule[]
  admission_fee_structures          admission_fee_structures[]
  admission_merit_rules             admission_merit_rules[]
  admission_number_sequences        admission_number_sequences?
  admission_question_bank           admission_question_bank[]
  admission_subjects                admission_subjects[]
  admission_visitors                admission_visitors[]
  admissions                        admissions[]
  approval_history                  approval_history[]
  assessment_accreditation_reports  assessment_accreditation_reports[]
  assessment_analytics_snapshots    assessment_analytics_snapshots[]
  assessment_assets                 assessment_assets[]
  assessment_benchmark_reports      assessment_benchmark_reports[]
  assessment_bloom_analytics        assessment_bloom_analytics[]
  assessment_certificate_templates  assessment_certificate_templates[]
  assessment_co_attainment          assessment_co_attainment[]
  assessment_configurations         assessment_configurations?
  assessment_dashboard_cache        assessment_dashboard_cache[]
  assessment_evaluation_assignments assessment_evaluation_assignments[]
  assessment_evaluation_sessions    assessment_evaluation_sessions[]
  assessment_folders                assessment_folders[]
  assessment_generated_papers       assessment_generated_papers[]
  assessment_generation_jobs        assessment_generation_jobs[]
  assessment_grace_rules            assessment_grace_rules[]
  assessment_grade_calculations     assessment_grade_calculations[]
  assessment_grading_scales         assessment_grading_scales[]
  assessment_import_logs            assessment_import_logs[]
  assessment_official_results       assessment_official_results[]
  assessment_po_attainment          assessment_po_attainment[]
  assessment_published_papers       assessment_published_papers[]
  assessment_quality_audits         assessment_quality_audits[]
  assessment_report_exports         assessment_report_exports[]
  assessment_result_sessions        assessment_result_sessions[]
  assessment_rubric_templates       assessment_rubric_templates[]
  assessment_rubrics                assessment_rubrics[]
  assessment_sessions               assessment_sessions[]
  assessment_workflow_definitions   assessment_workflow_definitions[]
  attendance_calendars              attendance_calendars[]
  attendance_devices                attendance_devices[]
  attendance_holidays               attendance_holidays[]
  attendance_logs                   attendance_logs[]
  attendance_makeup_classes         attendance_makeup_classes[]
  attendance_policies               attendance_policies[]
  attendance_special_days           attendance_special_days[]
  business_rules                    business_rules[]
  classes                           classes[]
  departments                       departments[]
  document_checklists               document_checklists[]
  exam_attendance                   exam_attendance[]
  exam_buildings                    exam_buildings[]
  exam_centers                      exam_centers[]
  exam_halls                        exam_halls[]
  exam_registrations                exam_registrations[]
  exam_result_publications          exam_result_publications[]
  exam_rooms                        exam_rooms[]
  exams                             exams[]
  feature_flags                     feature_flags[]
  finance_audit_logs                finance_audit_logs[]
  finance_fee_structures            finance_fee_structures[]
  finance_settings                  finance_settings?
  graduation_requirements           graduation_requirements[]
  hall_ticket_logs                  hall_ticket_logs[]
  hall_tickets                      hall_tickets[]
  import_jobs                       import_jobs[]
  invigilator_assignments           invigilator_assignments[]
  invigilator_availability          invigilator_availability[]
  notification_delivery_logs        notification_delivery_logs[]
  notification_queue                notification_queue[]
  notification_templates            notification_templates[]
  registration_documents            registration_documents[]
  registration_logs                 registration_logs[]
  registration_status_history       registration_status_history[]
  result_audit_logs                 result_audit_logs[]
  result_publications               result_publications[]
  result_rules                      result_rules[]
  schedule_rooms                    schedule_rooms[]
  schedule_sessions                 schedule_sessions[]
  seat_allocation_audit_logs        seat_allocation_audit_logs[]
  seat_allocations                  seat_allocations[]
  seat_layouts                      seat_layouts[]
  seat_templates                    seat_templates[]
  student_academic_records          student_academic_records[]
  student_fee_ledger                student_fee_ledger[]
  student_roll_number_sequences     student_roll_number_sequences[]
  student_subject_results           student_subject_results[]
  student_year_summaries            student_year_summaries[]
  students                          students[]
  subjects                          subjects[]
  timetable_slots                   timetable_slots[]
  transport_drivers                 transport_drivers[]
  transport_fee_slabs               transport_fee_slabs[]
  transport_stops                   transport_stops[]
  transport_trips                   transport_trips[]
  users                             public_users[]
  workflow_runs                     workflow_runs[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model seat_allocation_audit_logs {
  id               String            @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String            @db.Uuid
  allocation_id    String?           @db.Uuid
  action           String
  old_seat         String?
  new_seat         String?
  performed_by     String?           @db.Uuid
  remarks          String?
  created_at       DateTime          @default(now()) @db.Timestamptz(6)
  seat_allocations seat_allocations? @relation(fields: [allocation_id], references: [id], onUpdate: NoAction)
  users            public_users?     @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  schools          schools           @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model seat_allocations {
  id                         String                       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                  String                       @db.Uuid
  exam_schedule_id           String                       @db.Uuid
  student_id                 String                       @db.Uuid
  room_id                    String                       @db.Uuid
  seat_number                String
  status                     String?                      @default("ALLOCATED")
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  seat_allocation_audit_logs seat_allocation_audit_logs[]
  exam_rooms                 exam_rooms                   @relation(fields: [room_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                    schools                      @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students                   students                     @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([exam_schedule_id, student_id], map: "unique_student_schedule_seat_allocation")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model seat_layouts {
  id             String          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id      String          @db.Uuid
  room_id        String          @db.Uuid
  template_id    String?         @db.Uuid
  name           String
  created_at     DateTime        @default(now()) @db.Timestamptz(6)
  updated_at     DateTime        @default(now()) @db.Timestamptz(6)
  exam_rooms     exam_rooms      @relation(fields: [room_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools        schools         @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  seat_templates seat_templates? @relation(fields: [template_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model seat_templates {
  id            String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id     String         @db.Uuid
  name          String
  rows_count    Int
  cols_count    Int
  layout_matrix Json           @default("[]")
  created_at    DateTime       @default(now()) @db.Timestamptz(6)
  updated_at    DateTime       @default(now()) @db.Timestamptz(6)
  seat_layouts  seat_layouts[]
  schools       schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model section_faculty_assignments {
  id                                                   String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  section_id                                           String         @db.Uuid
  faculty_id                                           String         @db.Uuid
  academic_year_id                                     String         @db.Uuid
  assigned_at                                          DateTime?      @default(now()) @db.Timestamptz(6)
  assigned_by                                          String?        @db.Uuid
  status                                               String?        @default("ACTIVE")
  academic_years                                       academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users_section_faculty_assignments_assigned_byTousers public_users?  @relation("section_faculty_assignments_assigned_byTousers", fields: [assigned_by], references: [id], onUpdate: NoAction)
  users_section_faculty_assignments_faculty_idTousers  public_users   @relation("section_faculty_assignments_faculty_idTousers", fields: [faculty_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sections                                             sections       @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([section_id, faculty_id, academic_year_id, status])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sections {
  id                          String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  class_id                    String                        @db.Uuid
  name                        String
  created_at                  DateTime?                     @default(now()) @db.Timestamptz(6)
  faculty_section_subjects    faculty_section_subjects[]
  faculty_sections            faculty_sections[]
  section_faculty_assignments section_faculty_assignments[]
  classes                     classes                       @relation(fields: [class_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  student_faculty_assignments student_faculty_assignments[]
  student_sections            student_sections[]
  timetable_slots             timetable_slots[]

  @@unique([class_id, name])
  @@index([class_id], map: "idx_sections_class_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model staff_profiles {
  id            String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  user_id       String?       @unique @db.Uuid
  department_id String?       @db.Uuid
  staff_type    String
  joining_date  DateTime?     @db.Date
  status        String?       @default("active")
  created_at    DateTime?     @default(now()) @db.Timestamptz(6)
  designation   String?
  departments   departments?  @relation(fields: [department_id], references: [id], onUpdate: NoAction)
  users         public_users? @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model status_history {
  id             String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  entity_name    String
  entity_id      String        @db.Uuid
  old_status     String?
  new_status     String
  changed_by     String?       @db.Uuid
  reason         String?
  metadata       Json?
  correlation_id String?       @db.Uuid
  event_name     String?
  created_at     DateTime?     @default(now()) @db.Timestamptz(6)
  users          public_users? @relation(fields: [changed_by], references: [id], onUpdate: NoAction)

  @@index([correlation_id], map: "idx_status_history_correlation")
  @@index([entity_name, entity_id], map: "idx_status_history_lookup")
  @@schema("public")
}

model student_academic_record_snapshots {
  id                       String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  academic_record_id       String                   @db.Uuid
  snapshot_json            Json                     @default("{}")
  snapshot_hash            String
  created_at               DateTime                 @default(now()) @db.Timestamptz(6)
  student_academic_records student_academic_records @relation(fields: [academic_record_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_academic_records {
  id                                String                              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id                        String                              @db.Uuid
  academic_year_id                  String                              @db.Uuid
  grade                             String
  gpa_or_marks                      String?
  remarks                           String?
  created_at                        DateTime?                           @default(now()) @db.Timestamptz(6)
  school_id                         String?                             @db.Uuid
  student_academic_record_snapshots student_academic_record_snapshots[]
  academic_years                    academic_years                      @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                           schools?                            @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students                          students                            @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  student_academic_terms            student_academic_terms[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_academic_standing {
  id               String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id       String   @db.Uuid
  current_standing String   @default("GOOD_STANDING")
  updated_at       DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model student_academic_subjects {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  academic_term_id       String                 @db.Uuid
  subject_id             String                 @db.Uuid
  grade_point            Decimal                @default(0.00) @db.Decimal(4, 2)
  grade_label            String
  status                 String                 @default("PASS")
  student_academic_terms student_academic_terms @relation(fields: [academic_term_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model student_academic_terms {
  id                        String                      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  academic_record_id        String                      @db.Uuid
  term_id                   String                      @db.Uuid
  gpa                       Decimal                     @default(0.00) @db.Decimal(4, 2)
  earned_credits            Int                         @default(0)
  updated_at                DateTime                    @default(now()) @db.Timestamptz(6)
  student_academic_subjects student_academic_subjects[]
  student_academic_records  student_academic_records    @relation(fields: [academic_record_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_academic_timeline {
  id                String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id        String   @db.Uuid
  event_type        String
  event_description String
  created_at        DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_attendance_cache {
  id                    String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id            String         @db.Uuid
  academic_year_id      String         @db.Uuid
  term                  String?        @default("ANNUAL")
  total_working_days    Int?           @default(0)
  present_days          Int?           @default(0)
  attendance_percentage Decimal?       @default(dbgenerated("\nCASE\n    WHEN (total_working_days > 0) THEN round((((present_days)::numeric / (total_working_days)::numeric) * (100)::numeric), 2)\n    ELSE (0)::numeric\nEND")) @db.Decimal(5, 2)
  source                String?        @default("REAL")
  updated_at            DateTime?      @default(now()) @db.Timestamptz(6)
  last_updated_by       String?        @db.Uuid
  academic_years        academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 auth_users?    @relation(fields: [last_updated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  students              students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([student_id, academic_year_id, term])
  @@index([student_id], map: "idx_attend_cache_student")
  @@index([academic_year_id], map: "idx_attend_cache_year")
  @@schema("public")
}

model student_attendance_statistics {
  id                 String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id         String   @db.Uuid
  overall_percentage Decimal  @default(100.00) @db.Decimal(5, 2)
  updated_at         DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

model student_attendance_summary {
  id                    String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id            String         @db.Uuid
  academic_year_id      String         @db.Uuid
  month                 Int
  total_present         Int            @default(0)
  total_absent          Int            @default(0)
  total_late            Int            @default(0)
  attendance_percentage Decimal        @default(0.00) @db.Decimal(5, 2)
  last_calculated       DateTime?      @default(now()) @db.Timestamptz(6)
  academic_years        academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students              students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([student_id, academic_year_id, month], map: "unique_student_monthly_summary")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_barcodes {
  id            String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id    String    @db.Uuid
  barcode_value String    @unique
  symbology     String    @default("CODE128")
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  students      students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_class_allocations {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id       String         @unique @db.Uuid
  academic_year_id String         @db.Uuid
  grade            String
  section_id       String         @db.Uuid
  roll_number      Int
  allocated_at     DateTime?      @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([academic_year_id, grade, section_id, roll_number], map: "unique_section_roll")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_documents {
  id                  String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id          String              @db.Uuid
  document_id         String              @db.Uuid
  document_type       String
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  admission_documents admission_documents @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students            students            @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_exit_records {
  id           String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id   String        @unique @db.Uuid
  exit_type    String
  exit_date    DateTime      @db.Date
  reason       String?
  processed_by String?       @db.Uuid
  created_at   DateTime?     @default(now()) @db.Timestamptz(6)
  users        public_users? @relation(fields: [processed_by], references: [id], onUpdate: NoAction)
  students     students      @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_faculty_assignments {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id       String         @db.Uuid
  faculty_id       String         @db.Uuid
  section_id       String         @db.Uuid
  academic_year_id String         @db.Uuid
  source           String?        @default("SECTION_AUTO")
  status           String?        @default("ACTIVE")
  created_at       DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?      @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users            public_users   @relation(fields: [faculty_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sections         sections       @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([student_id, faculty_id, academic_year_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_fee_clearance_cache {
  id                 String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id         String         @db.Uuid
  academic_year_id   String         @db.Uuid
  term               String?        @default("ANNUAL")
  is_cleared         Boolean?       @default(false)
  outstanding_amount Decimal?       @default(0.00) @db.Decimal(10, 2)
  last_checked_at    DateTime?      @default(now()) @db.Timestamptz(6)
  source             String?        @default("REAL")
  fee_status         String?        @default("PENDING")
  last_updated_by    String?        @db.Uuid
  remarks            String?
  academic_years     academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users              auth_users?    @relation(fields: [last_updated_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  students           students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([student_id, academic_year_id, term])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_fee_ledger {
  id                     String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String?                 @db.Uuid
  student_id             String?                 @db.Uuid
  transaction_type       String
  debit                  Decimal                 @default(0.00) @db.Decimal(10, 2)
  credit                 Decimal                 @default(0.00) @db.Decimal(10, 2)
  running_balance        Decimal                 @default(0.00) @db.Decimal(10, 2)
  reference_type         String
  reference_id           String                  @db.Uuid
  created_at             DateTime?               @default(now()) @db.Timestamptz(6)
  description            String?
  performed_by           String?                 @db.Uuid
  school_id              String?                 @db.Uuid
  admission_applications admission_applications? @relation(fields: [application_id], references: [id], onUpdate: NoAction)
  users                  auth_users?             @relation(fields: [performed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  schools                schools?                @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students               students?               @relation(fields: [student_id], references: [id], onUpdate: NoAction)

  @@index([application_id], map: "idx_student_fee_ledger_application")
  @@index([student_id], map: "idx_student_fee_ledger_student")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_fees {
  id                  String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id          String                @db.Uuid
  fee_structure_id    String                @db.Uuid
  assigned_amount     Decimal               @db.Decimal(10, 2)
  created_at          DateTime?             @default(now()) @db.Timestamptz(6)
  fee_type            String?               @default("OTHER")
  reference_id        String?               @db.Uuid
  status              String?               @default("UNPAID")
  paid_amount         Decimal?              @default(0.00) @db.Decimal(10, 2)
  fee_status          String?               @default("UNPAID")
  payment_allocations payment_allocations[]
  students            students              @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([student_id, fee_structure_id])
  @@index([student_id], map: "idx_student_fees_student_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_guardians {
  id            String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id    String    @db.Uuid
  guardian_name String
  relation      String
  mobile_number String
  email         String?
  address       String?
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  students      students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model student_honors_history {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id  String   @db.Uuid
  honor_title String
  awarded_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_identity_cards {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id  String    @unique @db.Uuid
  barcode     String    @unique
  qr_code     String?
  printed     Boolean   @default(false)
  issued_date DateTime? @db.Date
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  students    students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model student_leave_requests {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String   @db.Uuid
  start_date DateTime @db.Date
  end_date   DateTime @db.Date
  leave_type String
  reason     String
  status     String   @default("PENDING")

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_parents {
  student_id     String       @db.Uuid
  parent_user_id String       @db.Uuid
  relation       String?      @default("guardian")
  created_at     DateTime?    @default(now()) @db.Timestamptz(6)
  users          public_users @relation(fields: [parent_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students       students     @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([student_id, parent_user_id])
  @@schema("public")
}

model student_probation_history {
  id         String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String    @db.Uuid
  reason     String
  placed_at  DateTime  @default(now()) @db.Timestamptz(6)
  cleared_at DateTime? @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_profiles {
  id                 String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id         String    @unique @db.Uuid
  date_of_birth      DateTime  @db.Date
  gender             String
  blood_group        String?
  nationality        String?
  religion           String?
  category           String?
  aadhaar            String?
  photo_url          String?
  allergies          String?
  medical_conditions String?
  emergency_notes    String?
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)
  students           students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_promotions {
  id                                                                      String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id                                                              String         @db.Uuid
  from_academic_year_id                                                   String         @db.Uuid
  to_academic_year_id                                                     String         @db.Uuid
  from_grade                                                              String
  to_grade                                                                String
  from_section_id                                                         String?        @db.Uuid
  to_section_id                                                           String?        @db.Uuid
  promoted_by                                                             String?        @db.Uuid
  promoted_at                                                             DateTime?      @default(now()) @db.Timestamptz(6)
  promotion_reason                                                        String
  academic_years_student_promotions_from_academic_year_idToacademic_years academic_years @relation("student_promotions_from_academic_year_idToacademic_years", fields: [from_academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                                                                   public_users?  @relation(fields: [promoted_by], references: [id], onUpdate: NoAction)
  students                                                                students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  academic_years_student_promotions_to_academic_year_idToacademic_years   academic_years @relation("student_promotions_to_academic_year_idToacademic_years", fields: [to_academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_provisioning_jobs {
  id                     String                 @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  application_id         String                 @db.Uuid
  step_name              String
  status                 String                 @default("PENDING")
  error_message          String?
  created_at             DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?              @default(now()) @db.Timestamptz(6)
  admission_applications admission_applications @relation(fields: [application_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([application_id, step_name], map: "unique_job_step")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_result_summaries {
  id             String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  exam_id        String    @db.Uuid
  student_id     String    @db.Uuid
  total_obtained Decimal?  @default(0) @db.Decimal
  total_max      Decimal?  @default(0) @db.Decimal
  percentage     Decimal?  @default(0) @db.Decimal
  grade          String?
  result_status  String?
  is_published   Boolean?  @default(false)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)
  published_at   DateTime? @db.Timestamptz(6)
  published_by   String?   @db.Uuid
  exams          exams     @relation(fields: [exam_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students       students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([exam_id, student_id], map: "unique_exam_student_result")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_roll_number_sequences {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String         @db.Uuid
  academic_year_id String         @db.Uuid
  grade            String
  section_id       String         @db.Uuid
  current_value    Int            @default(1)
  created_at       DateTime?      @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools          schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([school_id, academic_year_id, grade, section_id], map: "unique_section_sequence")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_section_history {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id       String         @db.Uuid
  from_section_id  String?        @db.Uuid
  to_section_id    String         @db.Uuid
  grade            String
  academic_year_id String         @db.Uuid
  transferred_at   DateTime?      @default(now()) @db.Timestamptz(6)
  reason           String?
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_sections {
  student_id       String          @db.Uuid
  section_id       String          @db.Uuid
  assigned_at      DateTime?       @default(now()) @db.Timestamptz(6)
  academic_year_id String?         @db.Uuid
  academic_years   academic_years? @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sections         sections        @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students        @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([student_id, section_id])
  @@unique([student_id, academic_year_id], map: "uni_student_year_section")
  @@index([section_id], map: "idx_student_sections_section_id")
  @@index([student_id], map: "idx_student_sections_student_id")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_status_history {
  id         String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String        @db.Uuid
  old_status String
  new_status String
  reason     String?
  changed_by String?       @db.Uuid
  changed_at DateTime?     @default(now()) @db.Timestamptz(6)
  users      public_users? @relation(fields: [changed_by], references: [id], onUpdate: NoAction)
  students   students      @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_subject_results {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String         @db.Uuid
  student_id       String         @db.Uuid
  subject_id       String         @db.Uuid
  academic_year_id String         @db.Uuid
  exam_term        String
  marks_obtained   Decimal        @db.Decimal(6, 2)
  total_marks      Decimal        @db.Decimal(6, 2)
  grade            String
  grade_points     Decimal        @db.Decimal(4, 2)
  status           String         @default("PASS")
  created_at       DateTime       @default(now()) @db.Timestamptz(6)
  updated_at       DateTime       @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools          schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects         subjects       @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([student_id, subject_id, exam_term], map: "unique_student_subject_term")
  @@index([student_id], map: "idx_student_subject_results_student")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_transfer_requests {
  id                 String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id         String    @db.Uuid
  destination_school String
  reason             String
  requested_at       DateTime? @default(now()) @db.Timestamptz(6)
  status             String    @default("PENDING")
  students           students  @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_transport_attendance {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id      String    @db.Uuid
  trip_date       DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  pickup_disabled Boolean?  @default(false)
  drop_disabled   Boolean?  @default(false)
  reason          String?
  marked_by       String?   @default(dbgenerated("auth.uid()")) @db.Uuid
  marked_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@unique([student_id, trip_date], map: "unique_transport_attendance_day")
  @@index([trip_date, student_id], map: "idx_transport_attendance_filter")
  @@schema("public")
}

model student_warning_history {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String   @db.Uuid
  reason     String
  issued_at  DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_workflow_rules {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_status String
  to_status   String
  role        String
  allowed     Boolean? @default(true)

  @@unique([from_status, to_status, role], map: "unique_student_workflow")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model student_year_summaries {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String         @db.Uuid
  student_id       String         @db.Uuid
  academic_year_id String         @db.Uuid
  gpa              Decimal?       @db.Decimal(4, 2)
  cgpa             Decimal?       @db.Decimal(4, 2)
  credits_earned   Int?           @default(0)
  total_credits    Int?           @default(0)
  rank             Int?
  percentile       Decimal?       @db.Decimal(5, 2)
  created_at       DateTime       @default(now()) @db.Timestamptz(6)
  updated_at       DateTime       @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools          schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  students         students       @relation(fields: [student_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([student_id, academic_year_id], map: "unique_student_year_summary")
  @@index([student_id], map: "idx_student_year_summaries_student")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model students {
  id                          String                        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                   String                        @db.Uuid
  admission_id                String                        @unique @db.Uuid
  student_code                String
  full_name                   String
  date_of_birth               DateTime                      @db.Date
  gender                      String?
  status                      String?                       @default("active")
  created_at                  DateTime?                     @default(now()) @db.Timestamptz(6)
  updated_at                  DateTime?                     @default(now()) @db.Timestamptz(6)
  email                       String?
  phone                       String?
  address                     String?
  user_id                     String?                       @db.Uuid
  admission_no                String?
  first_name                  String?
  last_name                   String?
  academic_year_id            String?                       @db.Uuid
  deleted_at                  DateTime?                     @db.Timestamptz(6)
  crm_application_id          String?                       @db.Uuid
  assessment_attempts         assessment_attempts[]
  assignment_submissions      assignment_submissions[]
  exam_attendance             exam_attendance[]
  exam_eligibility_snapshots  exam_eligibility_snapshots[]
  exam_registrations          exam_registrations[]
  exam_seating_allocations    exam_seating_allocations[]
  exam_violation_log          exam_violation_log[]
  fee_reminders               fee_reminders[]
  marks                       marks[]
  payment_transactions        payment_transactions[]
  payments                    payments[]
  seat_allocations            seat_allocations[]
  student_academic_records    student_academic_records[]
  student_attendance_cache    student_attendance_cache[]
  student_attendance_summary  student_attendance_summary[]
  student_barcodes            student_barcodes[]
  student_class_allocations   student_class_allocations?
  student_documents           student_documents[]
  student_exit_records        student_exit_records?
  student_faculty_assignments student_faculty_assignments[]
  student_fee_clearance_cache student_fee_clearance_cache[]
  student_fee_ledger          student_fee_ledger[]
  student_fees                student_fees[]
  student_guardians           student_guardians[]
  student_identity_cards      student_identity_cards?
  student_parents             student_parents[]
  student_profiles            student_profiles?
  student_promotions          student_promotions[]
  student_result_summaries    student_result_summaries[]
  student_section_history     student_section_history[]
  student_sections            student_sections[]
  student_status_history      student_status_history[]
  student_subject_results     student_subject_results[]
  student_transfer_requests   student_transfer_requests[]
  student_year_summaries      student_year_summaries[]
  academic_years              academic_years?               @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admissions                  admissions                    @relation(fields: [admission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  admission_applications      admission_applications?       @relation(fields: [crm_application_id], references: [id], onUpdate: NoAction)
  schools                     schools                       @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                       public_users?                 @relation(fields: [user_id], references: [id], onUpdate: NoAction)
  transport_trip_events       transport_trip_events[]

  @@unique([school_id, student_code])
  @@index([academic_year_id], map: "idx_students_academic_year")
  @@index([admission_id], map: "idx_students_admission_id")
  @@index([school_id], map: "idx_students_school")
  @@index([school_id], map: "idx_students_school_id")
  @@index([status], map: "idx_students_status")
  @@index([user_id], map: "idx_students_user_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model subjects {
  id                                String                              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                         String                              @db.Uuid
  class_id                          String                              @db.Uuid
  name                              String
  code                              String?
  created_at                        DateTime?                           @default(now()) @db.Timestamptz(6)
  type                              String?                             @default("theory")
  credits                           Decimal?                            @default(0) @db.Decimal(4, 2)
  assessment_evaluation_assignments assessment_evaluation_assignments[]
  assessment_generated_papers       assessment_generated_papers[]
  assessment_grace_rules            assessment_grace_rules[]
  assessment_published_papers       assessment_published_papers[]
  exam_subjects                     exam_subjects[]
  faculty_section_subjects          faculty_section_subjects[]
  marks                             marks[]
  student_subject_results           student_subject_results[]
  classes                           classes                             @relation(fields: [class_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools                           schools                             @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  timetable_slots                   timetable_slots[]

  @@unique([class_id, name])
  @@index([class_id], map: "idx_subjects_class_id")
  @@schema("public")
}

model task_attachments {
  id          String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  task_id     String?       @db.Uuid
  file_name   String
  file_url    String
  uploaded_by String?       @db.Uuid
  created_at  DateTime?     @default(now()) @db.Timestamptz(6)
  tasks       tasks?        @relation(fields: [task_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users       public_users? @relation(fields: [uploaded_by], references: [id], onUpdate: NoAction)

  @@schema("public")
}

model task_comments {
  id         String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  task_id    String?       @db.Uuid
  user_id    String?       @db.Uuid
  comment    String
  created_at DateTime?     @default(now()) @db.Timestamptz(6)
  tasks      tasks?        @relation(fields: [task_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users      public_users? @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tasks {
  id                  String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  title               String
  description         String?
  assigned_to         String?            @db.Uuid
  assigned_role       String?
  priority            String             @default("medium")
  status              String             @default("pending")
  due_at              DateTime?          @db.Timestamptz(6)
  related_entity_type String?
  related_entity_id   String?            @db.Uuid
  created_at          DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?          @default(now()) @db.Timestamptz(6)
  task_attachments    task_attachments[]
  task_comments       task_comments[]
  users               public_users?      @relation(fields: [assigned_to], references: [id], onUpdate: NoAction)

  @@index([assigned_role, status], map: "idx_tasks_assigned_role")
  @@index([assigned_to, status], map: "idx_tasks_assigned_to")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model timetable_slots {
  id               String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String         @db.Uuid
  academic_year_id String         @db.Uuid
  section_id       String         @db.Uuid
  subject_id       String         @db.Uuid
  faculty_user_id  String         @db.Uuid
  day_of_week      Int
  start_time       DateTime       @db.Time(6)
  end_time         DateTime       @db.Time(6)
  created_at       DateTime?      @default(now()) @db.Timestamptz(6)
  academic_years   academic_years @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users            public_users   @relation(fields: [faculty_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools          schools        @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sections         sections       @relation(fields: [section_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  subjects         subjects       @relation(fields: [subject_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([faculty_user_id], map: "idx_timetable_slots_faculty_id")
  @@index([section_id], map: "idx_timetable_slots_section_id")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transcript_requests {
  id         String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  student_id String   @db.Uuid
  status     String   @default("Requested")
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model transcript_signatures {
  id                   String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  transcript_id        String               @db.Uuid
  signatory_role       String
  signed_hash          String
  signed_at            DateTime             @default(now()) @db.Timestamptz(6)
  official_transcripts official_transcripts @relation(fields: [transcript_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model transcript_versions {
  id                   String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  transcript_id        String               @db.Uuid
  version_number       Int                  @default(1)
  snapshot_hash        String
  official_transcripts official_transcripts @relation(fields: [transcript_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_audit_logs {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  entity_type String
  entity_id   String    @db.Uuid
  action      String
  old_data    Json?
  new_data    Json?
  changed_at  DateTime? @default(now()) @db.Timestamptz(6)
  changed_by  String?   @default(dbgenerated("auth.uid()")) @db.Uuid

  @@index([changed_at], map: "idx_transport_audit_date")
  @@index([entity_type, entity_id], map: "idx_transport_audit_entity")
  @@index([entity_type, entity_id, changed_at], map: "idx_transport_audit_lookup")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_compliance_documents {
  id          String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  entity_type String
  entity_id   String    @db.Uuid
  doc_type    String
  doc_url     String?
  expiry_date DateTime? @db.Date
  is_verified Boolean?  @default(false)
  uploaded_at DateTime? @default(now()) @db.Timestamptz(6)
  uploaded_by String?   @default(dbgenerated("auth.uid()")) @db.Uuid

  @@index([entity_type, entity_id], map: "idx_transport_compliance_entity")
  @@index([expiry_date], map: "idx_transport_compliance_expiry")
  @@index([is_verified], map: "idx_transport_compliance_verified")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_drivers {
  id              String            @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id       String            @db.Uuid
  user_id         String?           @db.Uuid
  license_number  String?
  phone           String?
  status          String?           @default("ACTIVE")
  created_at      DateTime?         @default(now()) @db.Timestamptz(6)
  route_vehicles  route_vehicles[]
  schools         schools           @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users           public_users?     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  transport_trips transport_trips[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_fee_slabs {
  id               String          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id        String          @db.Uuid
  academic_year_id String          @db.Uuid
  stop_id          String          @db.Uuid
  amount           Decimal         @default(0.00) @db.Decimal(10, 2)
  currency         String?         @default("INR")
  created_at       DateTime?       @default(now()) @db.Timestamptz(6)
  academic_years   academic_years  @relation(fields: [academic_year_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  schools          schools         @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  transport_stops  transport_stops @relation(fields: [stop_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([academic_year_id, stop_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_route_stops {
  id              String          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  route_id        String          @db.Uuid
  stop_id         String          @db.Uuid
  stop_order      Int
  morning_time    DateTime?       @db.Time(6)
  evening_time    DateTime?       @db.Time(6)
  transport_stops transport_stops @relation(fields: [stop_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([route_id, stop_id])
  @@unique([route_id, stop_order])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_stops {
  id                    String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id             String                  @db.Uuid
  name                  String
  latitude              Decimal?                @db.Decimal(10, 8)
  longitude             Decimal?                @db.Decimal(11, 8)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  transport_fee_slabs   transport_fee_slabs[]
  transport_route_stops transport_route_stops[]
  schools               schools                 @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  transport_trip_events transport_trip_events[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_trip_events {
  id              String           @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  trip_id         String           @db.Uuid
  event_type      String
  stop_id         String?          @db.Uuid
  student_id      String?          @db.Uuid
  latitude        Decimal?         @db.Decimal
  longitude       Decimal?         @db.Decimal
  timestamp       DateTime?        @default(now()) @db.Timestamptz(6)
  created_by      String?          @default(dbgenerated("auth.uid()")) @db.Uuid
  message         String?
  transport_stops transport_stops? @relation(fields: [stop_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  students        students?        @relation(fields: [student_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  transport_trips transport_trips  @relation(fields: [trip_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([trip_id, timestamp], map: "idx_transport_events_trip_time")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_trip_locations {
  id              String          @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  trip_id         String          @db.Uuid
  latitude        Decimal         @db.Decimal
  longitude       Decimal         @db.Decimal
  heading         Decimal?        @db.Decimal
  recorded_at     DateTime?       @default(now()) @db.Timestamptz(6)
  transport_trips transport_trips @relation(fields: [trip_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([recorded_at], map: "idx_transport_locations_cleanup")
  @@index([trip_id, recorded_at(sort: Desc)], map: "idx_trip_locations_latest")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_trips {
  id                       String                     @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  school_id                String                     @db.Uuid
  route_id                 String                     @db.Uuid
  vehicle_id               String                     @db.Uuid
  driver_id                String                     @db.Uuid
  trip_date                DateTime                   @default(dbgenerated("CURRENT_DATE")) @db.Date
  trip_type                String
  status                   String                     @default("SCHEDULED")
  started_at               DateTime?                  @db.Timestamptz(6)
  completed_at             DateTime?                  @db.Timestamptz(6)
  created_at               DateTime?                  @default(now()) @db.Timestamptz(6)
  transport_trip_events    transport_trip_events[]
  transport_trip_locations transport_trip_locations[]
  transport_drivers        transport_drivers          @relation(fields: [driver_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  schools                  schools                    @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([driver_id, trip_date, trip_type], map: "unique_driver_trip_schedule")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transport_vehicle_checks {
  id             String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  trip_id        String    @db.Uuid
  vehicle_id     String    @db.Uuid
  driver_id      String    @db.Uuid
  fuel_level     String?
  tyres_ok       Boolean?  @default(false)
  brakes_ok      Boolean?  @default(false)
  lights_ok      Boolean?  @default(false)
  cleanliness_ok Boolean?  @default(false)
  remarks        String?
  checked_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@index([trip_id], map: "idx_transport_checks_trip")
  @@index([vehicle_id, checked_at(sort: Desc)], map: "idx_transport_checks_vehicle")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_roles {
  user_id     String     @db.Uuid
  role_id     String     @db.Uuid
  assigned_at DateTime?  @default(now()) @db.Timestamptz(6)
  roles       roles      @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users       auth_users @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([user_id, role_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model public_users {
  id                                                                         String                                 @id @db.Uuid
  school_id                                                                  String?                                @db.Uuid
  full_name                                                                  String
  email                                                                      String
  status                                                                     String?                                @default("active")
  created_at                                                                 DateTime?                              @default(now()) @db.Timestamptz(6)
  login_status                                                               String?                                @default("PENDING")
  login_decision_reason                                                      String?
  login_decided_by                                                           String?                                @db.Uuid
  login_decided_at                                                           DateTime?                              @db.Timestamptz(6)
  academic_automation_logs                                                   academic_automation_logs[]
  admission_applications                                                     admission_applications[]
  admission_assessment_responses                                             admission_assessment_responses[]
  admission_audit_logs                                                       admission_audit_logs[]
  admission_confirmation                                                     admission_confirmation[]
  admission_enrollment_logs                                                  admission_enrollment_logs[]
  admission_exam_results                                                     admission_exam_results[]
  admission_fee_waivers                                                      admission_fee_waivers[]
  admission_followups                                                        admission_followups[]
  admission_leads                                                            admission_leads[]
  admission_visitors_admission_visitors_counselor_idTousers                  admission_visitors[]                   @relation("admission_visitors_counselor_idTousers")
  admission_visitors_admission_visitors_created_byTousers                    admission_visitors[]                   @relation("admission_visitors_created_byTousers")
  admissions                                                                 admissions[]
  application_documents_application_documents_uploaded_byTousers             application_documents[]                @relation("application_documents_uploaded_byTousers")
  application_documents_application_documents_verified_byTousers             application_documents[]                @relation("application_documents_verified_byTousers")
  application_workflow                                                       application_workflow[]
  approval_history                                                           approval_history[]
  approval_requests_approval_requests_assigned_user_idTousers                approval_requests[]                    @relation("approval_requests_assigned_user_idTousers")
  approval_requests_approval_requests_decided_byTousers                      approval_requests[]                    @relation("approval_requests_decided_byTousers")
  assessment_accreditation_reports                                           assessment_accreditation_reports[]
  assessment_blueprint_validation_logs                                       assessment_blueprint_validation_logs[]
  assessment_evaluation_annotations                                          assessment_evaluation_annotations[]
  assessment_evaluation_assignments                                          assessment_evaluation_assignments[]
  assessment_evaluation_locks                                                assessment_evaluation_locks[]
  assessment_evaluation_logs                                                 assessment_evaluation_logs[]
  assessment_evaluation_sessions                                             assessment_evaluation_sessions[]
  assessment_folders                                                         assessment_folders[]
  assessment_generated_exports                                               assessment_generated_exports[]
  assessment_generated_papers                                                assessment_generated_papers[]
  assessment_generated_validation_logs                                       assessment_generated_validation_logs[]
  assessment_generation_jobs                                                 assessment_generation_jobs[]
  assessment_generation_locks                                                assessment_generation_locks[]
  assessment_grade_cards                                                     assessment_grade_cards[]
  assessment_grade_history                                                   assessment_grade_history[]
  assessment_import_logs                                                     assessment_import_logs[]
  assessment_marks                                                           assessment_marks[]
  assessment_moderation_queue                                                assessment_moderation_queue[]
  assessment_promotion_decisions                                             assessment_promotion_decisions[]
  assessment_published_papers                                                assessment_published_papers[]
  assessment_quality_audits                                                  assessment_quality_audits[]
  assessment_question_evaluations                                            assessment_question_evaluations[]
  assessment_result_approval_workflow                                        assessment_result_approval_workflow[]
  assessment_result_audit_logs                                               assessment_result_audit_logs[]
  assessment_result_freeze_logs                                              assessment_result_freeze_logs[]
  assessment_result_publications                                             assessment_result_publications[]
  assessment_result_sessions                                                 assessment_result_sessions[]
  assessment_result_versions                                                 assessment_result_versions[]
  assessment_sync_conflicts                                                  assessment_sync_conflicts[]
  assessment_transcripts                                                     assessment_transcripts[]
  assessment_workflow_definitions                                            assessment_workflow_definitions[]
  assignment_submissions                                                     assignment_submissions[]
  attendance_logs                                                            attendance_logs[]
  attendance_record_versions                                                 attendance_record_versions[]
  attendance_session_locks                                                   attendance_session_locks[]
  attendance_session_workflow                                                attendance_session_workflow[]
  audit_logs                                                                 audit_logs[]
  document_comments                                                          document_comments[]
  document_verification                                                      document_verification[]
  document_versions                                                          document_versions[]
  exam_attendance                                                            exam_attendance[]
  exam_eligibility_snapshots                                                 exam_eligibility_snapshots[]
  exams                                                                      exams[]
  faculty_profiles                                                           faculty_profiles?
  faculty_section_subjects                                                   faculty_section_subjects[]
  faculty_sections                                                           faculty_sections[]
  hall_ticket_logs                                                           hall_ticket_logs[]
  import_jobs                                                                import_jobs[]
  marks                                                                      marks[]
  notification_queue                                                         notification_queue[]
  registration_logs                                                          registration_logs[]
  registration_status_history                                                registration_status_history[]
  request_tracking                                                           request_tracking[]
  result_audit_logs                                                          result_audit_logs[]
  result_publications                                                        result_publications[]
  seat_allocation_audit_logs                                                 seat_allocation_audit_logs[]
  section_faculty_assignments_section_faculty_assignments_assigned_byTousers section_faculty_assignments[]          @relation("section_faculty_assignments_assigned_byTousers")
  section_faculty_assignments_section_faculty_assignments_faculty_idTousers  section_faculty_assignments[]          @relation("section_faculty_assignments_faculty_idTousers")
  staff_profiles                                                             staff_profiles?
  status_history                                                             status_history[]
  student_exit_records                                                       student_exit_records[]
  student_faculty_assignments                                                student_faculty_assignments[]
  student_parents                                                            student_parents[]
  student_promotions                                                         student_promotions[]
  student_status_history                                                     student_status_history[]
  students                                                                   students[]
  task_attachments                                                           task_attachments[]
  task_comments                                                              task_comments[]
  tasks                                                                      tasks[]
  timetable_slots                                                            timetable_slots[]
  transport_drivers                                                          transport_drivers[]
  auth_user                                                                  auth_users                             @relation(fields: [id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  login_decided_by_user                                                      public_users?                          @relation("usersTousers", fields: [login_decided_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_users                                                                public_users[]                         @relation("usersTousers")
  schools                                                                    schools?                               @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  workflow_logs                                                              workflow_logs[]

  @@index([school_id], map: "idx_users_school_id")
  @@map("users")
  @@schema("public")
}

model workflow_actions {
  id                  String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  version_id          String?            @db.Uuid
  node_id             String
  action_type         String
  action_order        Int                @default(1)
  action_config       Json               @default("{}")
  retry_count         Int                @default(3)
  retry_interval_secs Int                @default(60)
  created_at          DateTime?          @default(now()) @db.Timestamptz(6)
  workflow_versions   workflow_versions? @relation(fields: [version_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model workflow_conditions {
  id                String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  version_id        String?            @db.Uuid
  node_id           String
  rules_config      Json               @default("{}")
  created_at        DateTime?          @default(now()) @db.Timestamptz(6)
  workflow_versions workflow_versions? @relation(fields: [version_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model workflow_dlq {
  id              String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workflow_run_id String?        @db.Uuid
  node_id         String?
  action_type     String?
  error_message   String?
  payload         Json?
  retry_attempts  Int            @default(0)
  created_at      DateTime?      @default(now()) @db.Timestamptz(6)
  workflow_runs   workflow_runs? @relation(fields: [workflow_run_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

model workflow_logs {
  id              String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workflow_run_id String?        @db.Uuid
  node_id         String?
  action_taken    String
  performed_by    String?        @db.Uuid
  remarks         String?
  metadata        Json?
  created_at      DateTime?      @default(now()) @db.Timestamptz(6)
  users           public_users?  @relation(fields: [performed_by], references: [id], onUpdate: NoAction)
  workflow_runs   workflow_runs? @relation(fields: [workflow_run_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([workflow_run_id], map: "idx_workflow_logs_run")
  @@schema("public")
}

model workflow_notifications {
  id                String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  version_id        String?            @db.Uuid
  node_id           String
  channels          String[]
  template          String
  created_at        DateTime?          @default(now()) @db.Timestamptz(6)
  workflow_versions workflow_versions? @relation(fields: [version_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model workflow_rules {
  id          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  from_status String
  to_status   String
  role        String
  allowed     Boolean? @default(true)

  @@unique([from_status, to_status, role], map: "unique_workflow_rule")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model workflow_runs {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  version_id        String?             @db.Uuid
  entity_type       String
  entity_id         String              @db.Uuid
  school_id         String              @db.Uuid
  current_node_id   String?
  variables_context Json                @default("{}")
  status            String              @default("running")
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?           @default(now()) @db.Timestamptz(6)
  approval_requests approval_requests[]
  workflow_dlq      workflow_dlq[]
  workflow_logs     workflow_logs[]
  schools           schools             @relation(fields: [school_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  workflow_versions workflow_versions?  @relation(fields: [version_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([entity_type, entity_id], map: "idx_workflow_runs_entity")
  @@schema("public")
}

model workflow_steps {
  id                   String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  version_id           String?            @db.Uuid
  node_id              String
  name                 String
  role_required        String
  sequence_order       Int
  sla_warning_hours    Int?               @default(24)
  sla_escalation_hours Int?               @default(48)
  escalation_role      String?            @default("PRINCIPAL")
  created_at           DateTime?          @default(now()) @db.Timestamptz(6)
  workflow_versions    workflow_versions? @relation(fields: [version_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model workflow_variables {
  id                  String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workflow_version_id String?            @db.Uuid
  variable_name       String
  variable_type       String
  default_value       String?
  scope               String             @default("local")
  is_required         Boolean            @default(false)
  created_at          DateTime?          @default(now()) @db.Timestamptz(6)
  workflow_versions   workflow_versions? @relation(fields: [workflow_version_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model workflow_versions {
  id                     String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  workflow_id            String?                  @db.Uuid
  version                Int
  status                 String                   @default("draft")
  nodes                  Json                     @default("[]")
  connections            Json                     @default("[]")
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  workflow_actions       workflow_actions[]
  workflow_conditions    workflow_conditions[]
  workflow_notifications workflow_notifications[]
  workflow_runs          workflow_runs[]
  workflow_steps         workflow_steps[]
  workflow_variables     workflow_variables[]
  workflows              workflows?               @relation(fields: [workflow_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([workflow_id, version], map: "unique_workflow_version")
  @@index([workflow_id, status], map: "idx_workflow_versions_lookup")
  @@schema("public")
}

model workflows {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  name              String
  code              String              @unique
  description       String?
  is_template       Boolean             @default(false)
  created_at        DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?           @default(now()) @db.Timestamptz(6)
  workflow_versions workflow_versions[]

  @@schema("public")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}
