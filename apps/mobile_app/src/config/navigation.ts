import { ROUTES } from '../constants/routes';

export const NAVIGATION_CONFIG = {
  initialRoute: ROUTES.AUTH.SPLASH,
  defaultAuthSuccessRoute: ROUTES.TABS.DASHBOARD,
  defaultLogoutRoute: ROUTES.AUTH.LOGIN,
};
